# ATDO Execution Plan — init 子系统开发

> 目标：完成 `ae init` 命令的全部开发，从零到可用的端到端交付
> 设计基线：`@ design/v1.0-INIT.md`（已评审 7.4/10，P0 bug 已修）
> 模板资产：`@ design/v1.0-TEMPLATES.md`
> 共享契约：`@ design/v1.0-SHARED.md`
> 总估算：~2100 行 Python + ~800 行 Jinja2 模板
> 日期：2026-06-24

---

## Phase 0：环境准备

**目标**：确保依赖和目录结构就绪。

**操作**：
1. 确认 `pyproject.toml` 包含所有依赖：`pyyaml`、`jinja2`、`click`（已有）+ **新增 `packaging`、`binaryornot`**
2. `uv sync` 安装全部依赖
3. 确认 `auto_engineering/init/templates/` 目录结构存在

**验证**：
```bash
uv run python -c "import yaml, jinja2, click, packaging; from binaryornot.check import is_binary; print('OK')"
```

**Gate**：所有依赖可导入 ✓

---

## Phase 1：错误体系 + 模板配置解析

**依赖**：Phase 0

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/init/errors.py` | 重写（替换 stub） | ~50 |
| `auto_engineering/init/config.py` | 重写（替换 stub） | ~200 |

**实现要点**（来自 `design/v1.0-INIT.md` §1.3.1 + §1.3.2）：

errors.py：
- `InitError` 基类 + 7 个子类
- 每个子类有 `exit_code: int`
- `TaskExecutionError` 带 command/returncode/stderr
- `TemplateRenderError` 带 src_path/jinja_error

config.py：
- `Question` dataclass：get_type_name() / render_when() / render_validator() / cast_answer()
- `Task` dataclass
- `TemplateConfig` dataclass：load() / _load_yaml_with_includes() / _parse_questions() / _parse_tasks()
- `DEFAULT_EXCLUDE`、`TEMPLATES_ROOT` 常量

**验证**：
```bash
uv run python -c "
from auto_engineering.init.errors import (
    InitError, ConfigFileError, UnsatisfiedPrerequisiteError,
    TargetDirectoryError, ValidationError, TaskExecutionError,
    TemplateRenderError, InitInterruptedError
); print('errors OK')
from auto_engineering.init.config import (
    TEMPLATES_ROOT, Question, Task, TemplateConfig
);
q = Question(var_name='test', default=True)
assert q.get_type_name() == 'bool'
assert q.cast_answer('yes') == True
print('config OK')
"
```

**Gate**：
- 所有错误类型可导入，exit_code 正确
- `Question.cast_answer()` 对 6 种类型正确转换
- `TemplateConfig.load()` 可被调用（即使 templates/ 目录为空返回空配置）
- 现有 stub 测试通过

---

## Phase 2：答案管理层

**依赖**：Phase 1

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/init/answers.py` | 重写（替换 stub） | ~120 |

**实现要点**（来自 §1.3.3）：
- `AnswersMap` dataclass：6 层 ChainMap + external_data 懒加载
- `get()` / `combined()` / `hide()` / `save_partial()` / `from_answers_file()` / `write_to()` / `to_answers_file()`
- `__getitem__` / `__contains__` 适配 dict-like 访问

**验证**：
```bash
uv run python -c "
from auto_engineering.init.answers import AnswersMap
a = AnswersMap(defaults={'k': 'v'}, cli_overrides={'x': 'y'})
assert a.get('k') == 'v'
assert a['x'] == 'y'  # cli overrides
assert 'k' in a
combined = a.combined()
assert combined['_ae_version'] == '1.0.0'
print('answers OK')
"
```

**Gate**：
- 优先级：cli > interactive > previous > defaults > builtins
- `save_partial()` / `from_answers_file()` 往返正确
- `write_to()` 生成的 YAML 可被 `from_answers_file()` 正确读取

---

## Phase 3：交互式问答

**依赖**：Phase 1 + Phase 2

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/init/prompts.py` | 重写（替换 stub） | ~150 |

**实现要点**（来自 §1.3.4）：
- `InteractivePrompt`：两遍循环（simple → complex）
- `_ask_one()`：when 跳过 → 类型推导 → cast → validator 循环
- `_render_default()`：Jinja2 模板渲染默认值
- `prompt_for_project_type()`：Click Choice 选择
- `_PROMPT_DISPATCH`：8 种类型 → Click 方法映射

**验证**：
```bash
# 非交互模式验证（无 stdin）
uv run python -c "
from auto_engineering.init.config import Question
from auto_engineering.init.answers import AnswersMap
from auto_engineering.init.prompts import InteractivePrompt

q1 = Question(var_name='name', type='str', default='test')
q2 = Question(var_name='flag', type='bool', default=True, when='{{ name == \"test\" }}')
answers = AnswersMap(defaults={'name': 'test', 'flag': True})
prompt = InteractivePrompt([q1, q2], answers)
# 在 defaults 模式下不应报错
print('prompts OK')
"
```

**Gate**：
- `InteractivePrompt.__init__()` 和 `run()` 可被调用
- 8 种类型映射完整
- when 条件渲染逻辑正确

---

## Phase 4：渲染引擎

**依赖**：Phase 1 + Phase 2

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/init/renderer.py` | 重写（替换 stub） | ~180 |

**实现要点**（来自 §1.3.5）：
- `TemplateRenderer`：SandboxedEnvironment + 多层模板目录 + 双层渲染
- `render_to()`：遍历 → 排除 → 文件名渲染 → no_render 判断 → 内容渲染/二进制复制
- 二进制检测（binaryornot + 降级方案）
- 换行符保持 + 文件权限保持
- `_is_excluded()` / `_is_no_render()` / `_should_overwrite()` 辅助方法

**验证**：
```bash
# 创建一个最小模板目录并验证渲染
mkdir -p /tmp/ae-test-template
echo "Hello {{ name }}" > /tmp/ae-test-template/hello.txt.jinja
uv run python -c "
from pathlib import Path
from auto_engineering.init.renderer import TemplateRenderer
import tempfile, shutil

r = TemplateRenderer(
    template_dirs=[Path('/tmp/ae-test-template')],
    context={'name': 'World'},
)
tmp = Path(tempfile.mkdtemp())
result = r.render_to(tmp)
assert (tmp / 'hello.txt').read_text() == 'Hello World'
shutil.rmtree(tmp)
print('renderer OK')
"
rm -rf /tmp/ae-test-template
```

**Gate**：
- Jinja2 模板内容正确渲染
- 二进制文件原样复制
- no_render 模式生效
- exclude 过滤生效

---

## Phase 5：钩子执行器 + 项目检测器

**依赖**：Phase 1（无相互依赖，可并行）

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/init/hooks.py` | 重写（替换 stub） | ~100 |
| `auto_engineering/init/detector.py` | 重写（替换 stub） | ~80 |

**实现要点**（来自 §1.3.6 + §1.3.7）：

hooks.py：
- `TaskRunner.__init__()` / `run()`
- when 条件渲染 → working_directory 解析 → extra_vars 双注入（Jinja + env）
- str 模式（shell）vs list 模式（安全）命令执行
- returncode != 0 → TaskExecutionError

detector.py：
- `FRAMEWORK_SIGNATURES` + `ADVANCED_CHECKS`
- `ProjectDetector.detect()` / `list_candidates()`
- package.json 深度检查（mcp-server 检查依赖，cli-tool 检查 bin）

**验证**：
```bash
uv run python -c "
from auto_engineering.init.hooks import TaskRunner
from auto_engineering.init.detector import ProjectDetector
from pathlib import Path

# detector: 当前项目目录应检测到 library 签名（有 pyproject.toml）
d = ProjectDetector(Path.cwd())
print(f'Detected: {d.detect()} (candidates: {d.list_candidates()})')
# hooks: TaskRunner 可实例化
r = TaskRunner(Path('/tmp'))
print('hooks + detector OK')
"
```

**Gate**：
- `ProjectDetector.detect()` 返回结果合理
- `TaskRunner.run()` 能执行简单任务（可 mock）
- 检测器签名覆盖 8 种项目类型

---

## Phase 6：中心编排器

**依赖**：Phase 1-5 全部

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/init/scaffold.py` | 重写（替换 stub） | ~300 |

**实现要点**（来自 §1.3.8）：
- `InitWorker` dataclass：context manager + 5 阶段流水线
- `_phase_detect()` → `_phase_prompt()` → `_check_template_version()` → `_phase_render()` → `_phase_tasks()` → 原子移动
- `_run_builtin_hooks()`：git init → install → lefthook → commit（全带错误检查 + git 版本兼容）
- `pretend` / `skip_tasks` / `cleanup_on_error` 参数控制
- `init_project()` 便捷函数

**验证**：
```bash
# 确保当前目录有 ae-template.yml（创建最小模板）
mkdir -p auto_engineering/init/templates/app-service
cat > auto_engineering/init/templates/app-service/ae-template.yml << 'YAML'
project_name:
  type: str
  help: 项目名称
  default: test-project
YAML

# 测试 dry-run（不产生文件）
uv run python -c "
from pathlib import Path
from auto_engineering.init.scaffold import InitWorker

w = InitWorker(
    dst_path=Path('/tmp/ae-test-target'),
    project_type='app-service',
    defaults=True,
    pretend=True,
    quiet=False,
)
with w:
    result = w.execute()
    print(f'Pretend OK: {len(result.files)} files would be generated')
print('scaffold OK')
"

# 清理
rm -rf /tmp/ae-test-target auto_engineering/init/templates
```

**Gate**：
- InitWorker 5 个 Phase 按顺序执行
- pretend=True 时不产生文件
- context manager 正确清理
- `_check_template_version()` 在 prompt 之后调用

---

## Phase 7：共享契约 + 环境解析

**依赖**：Phase 0

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/config/environment.py` | 重写（已有完整实现，审查确认） | ~120 |

**实现要点**（来自 §1.3.9）：
- `ProjectEnvironment.resolve()` / `_from_answers_file()` / `_from_detection()`
- `_sync_detectable()` / `_detect_package_manager()` / `_detect_test_runner()` / `_detect_ci()`
- `save()` 写回 .ae-answers.yml

**验证**：
```bash
uv run ae status
```

**Gate**：
- `ae status` 正确显示当前项目环境
- 有 .ae-answers.yml 时正确读取
- 无文件时自检测生成

---

## Phase 8：模板资产创建

**依赖**：Phase 6（模板目录在 scaffold 中有引用）

**文件**：

| 目录 | 操作 | 文件数 |
|------|------|:---:|
| `auto_engineering/init/templates/_shared/` | 创建 | 5 |
| `auto_engineering/init/templates/_features/` | 创建 | ~30 |
| `auto_engineering/init/templates/app-service/` | 创建 | 2（ae-template.yml + 源码骨架） |
| `auto_engineering/init/templates/library/` | 创建 | 2 |
| `auto_engineering/init/templates/cli-tool/` | 创建 | 2 |
| `auto_engineering/init/templates/skill/` | 创建 | 3 |
| `auto_engineering/init/templates/hook/` | 创建 | 3 |
| `auto_engineering/init/templates/mcp-server/` | 创建 | 2 |
| `auto_engineering/init/templates/spec-doc/` | 创建 | 1 |
| `auto_engineering/init/templates/monorepo/` | 创建 | 2 |

**模板内容来源**：`@ design/v1.0-TEMPLATES.md` §17.3（ae-template.yml）× §17.5（_features/）× §17.4（_shared/）

**验证**：
```bash
find auto_engineering/init/templates -type f | wc -l  # 期望 >= 40
uv run python -c "
from auto_engineering.init.config import TemplateConfig
for pt in ['app-service', 'library', 'cli-tool', 'skill', 'hook', 'mcp-server', 'spec-doc', 'monorepo']:
    cfg = TemplateConfig.load(pt)
    print(f'{pt}: {len(cfg.questions)} questions, {len(cfg.tasks_before)} before, {len(cfg.tasks_after)} after')
print('All templates OK')
"
```

**Gate**：
- 8 个 ae-template.yml 全部可解析
- 至少 40 个模板文件存在
- 每个类型的 questions 数量合理（>= 3）

---

## Phase 9：CLI 集成 + 端到端测试

**依赖**：Phase 1-8 全部

**文件**：

| 文件 | 操作 | 行数 |
|------|------|:---:|
| `auto_engineering/cli.py` | 修改（init 命令已有，审查完善） | ~20 |
| `tests/test_init.py` | 新增 | ~200 |

**实现要点**：
- `ae init --help` 输出完整
- `--from-answers` 正确注入 `InitWorker._previous_answers`
- `ae status` 正确显示

**验证**：
```bash
# 端到端测试
uv run ae init /tmp/ae-e2e-test \
    --type app-service \
    --defaults \
    --package-manager pnpm \
    --ci github \
    --pretend \
    --force

# 结果验证
echo "E2E test passed"
```

**Gate**：
- `ae init --help` 输出所有 11 个 flag
- E2E 测试生成的项目结构正确
- `ae status` 在生成的项目目录中正确显示

---

## Phase 依赖图

```
Phase 0 (env)
   │
   ├─→ Phase 1 (errors + config) ──→ Phase 2 (answers)
   │                                      │
   │                              ┌───────┘
   │                              ▼
   │                    Phase 3 (prompts)
   │                              │
   │                    ┌─────────┘
   │                    ▼
   │              Phase 4 (renderer)
   │                    │
   ├─→ Phase 5 (hooks + detector)
   │                    │
   │         ┌──────────┴──────────┐
   │         ▼                     ▼
   │   Phase 6 (scaffold)    Phase 7 (environment)
   │         │                     │
   │         └──────────┬──────────┘
   │                    ▼
   │              Phase 8 (templates)
   │                    │
   └────────────────────┴──→ Phase 9 (CLI + E2E)
```

## Gate 汇总

| Phase | Gate 条件 | 失败动作 |
|-------|----------|---------|
| 0 | 所有依赖可导入 | 修复 pyproject.toml，重新 uv sync |
| 1 | errors+config import 通过，cast_answer 正确 | 修复实现，此 Phase 是所有上游依赖 |
| 2 | AnswersMap 优先级链正确 | 修复 get/combined 逻辑 |
| 3 | InteractivePrompt 可实例化 | 修复 prompt dispatch |
| 4 | TemplateRenderer 正确渲染模板文件 | 检查 Jinja2 环境配置 |
| 5 | detector 返回合理结果 | 调整签名列表 |
| 6 | InitWorker 5 Phase 顺序执行 | 回退到上一步修复，不得回退超过 1 步 |
| 7 | `ae status` 正常 | 检查 environment.py |
| 8 | 全部 8 个 ae-template.yml 可解析 | 修复 YAML 语法 |
| 9 | `ae init` E2E 通过 | 检查 CLI 接线，不允许回退到 Phase 6 之前 |
