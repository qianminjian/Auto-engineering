# 真跑验证计划(2026-07-26)

> 状态:准备完成,等前置任务(Phase 47 T117/T118)完成后启动
> 用途:以真实需求开发验证 v5.6 tick 全路径,目标生产级交付。**会话压缩/新会话后,重新开跑前先读本文件恢复上下文**

## 1. 用户需求(原始 5 项)

1. 测试项目:`/Users/minjianq/Documents/66-Project/ClaudeCode/test-project/voice_clone_for_auto_CC_Design`(需求 `design/V1.0-Design-VoiceClonePage.md`),以 `/dev-loop "V1.0-Design-VoiceClonePage.md"` 方式跑,验证调度全路径
2. 按真实完整需求开发测试;发现 loop 问题 → 马上修复 → 重试;目标生产级结果;**所有提交给 LLM 的提示词统一输出到一个文件,待用户审核**
3. 启动参数:Audit:✓ Metrics:✓ OTLP:✓ LangSmith:✓ Debug:✓ Cache:✓(Standalone) PII:✓ Token:✓;跑完后报告输出到 **Auto-engineering 项目** `_scratch/test-output/`,报告文件名加调用启动方式后缀
4. 跑完后分析日志,核验 **T50 / T51a / T51b / T51c / T51d / T51e / T51f / T53 / T54 / T55** 在本次真跑中的实际情况是否符合设计目标,汇总问题形成完整报告
5. 真跑产生的 voice_clone 项目目录**不要删除**;启动开发出的项目服务待用户验证;**用户通知后再删除**

## 2. 确认结果(2026-07-26 用户已确认)

### 2.1 启动参数映射(核实后尽力全开)

| 用户参数 | Flag / 机制 | 状态 |
|---------|------------|------|
| Audit | `AE_AUDIT_LOG=1` | ✅ 可开 |
| Metrics | `AE_METRICS=1` | ✅ 可开 |
| OTLP | `AE_OTLP_ENDPOINT=http://localhost:4317` | ✅ 可开(需先起 collector;不可达时按修复后的 tracing 逻辑静默降级 NoOp,不阻断) |
| LangSmith | 无 flag | ⚠️ **当前版本无 langsmith 代码**(grep 全项目零命中;Phase 25 T93 记录"已落地",疑似 Phase 40 清理移除,真跑时 git log 查清)。按用户决策:记"本版不可用"入报告,或视查清结果定 |
| Debug | `AE_DEBUG=1` | ✅ 可开 |
| Cache | 无 flag(Anthropic 原生) | ✅ prompt caching 为 Anthropic 原生默认开启,无独立开关,报告说明;"(Standalone)" 为历史模式标记,StandaloneDriver 已 Phase 40 删除 |
| PII | `AE_PII_ENABLED=1` | ✅ 可开 |
| Token | `AE_TOKEN_TRACKING=1` | ✅ 可开 |

### 2.2 Prompt 汇总方式(用户确认:跑后汇总单文件)

保留现有 per-tick `log_prompt` 机制(写入 voice_clone `_scratch/prompt-log/`);**真跑结束后**全部提示词合并为单文件 `all-prompts-<时间戳>.md`,随报告放 `_scratch/test-output/` 待用户审核。

### 2.3 报告输出

- 目录:Auto-engineering `_scratch/test-output/`
- 命名:`<日期>-真跑报告-<启动方式后缀>.md`,如 `2026-07-XX-真跑报告-all-features-on.md`(后缀反映参数组合)

## 3. 开跑前检查清单(必须逐项过)

- [ ] **Phase 47 T117(collector 拆分)+ T118(mypy 清零)完成** — metrics 子系统稳定是 Metrics:✓ 真跑的前提
- [ ] LangSmith T93 来龙去脉查清(git log),决定"不可用"还是"修复"
- [ ] ae CLI 调用方式确认(`which ae` / `ae doctor` 7 项预检)
- [ ] /dev-loop 执行的项目根指向确认(在 voice_clone 目录开 Claude Code 会话跑 slash 命令,或 CLI 指定 root——commands/dev-loop.md 默认 cwd 为项目根,**当前会话 cwd 是 Auto-engineering,直接跑会开发错项目**)
- [ ] voice_clone 项目状态检查(是否有旧 `.ae-state/` 需清理、node_modules/pnpm 就绪)
- [ ] OTLP collector 启动决策(`ae doctor --setup-observability` 或 docker)
- [ ] 全量测试基线确认绿色(1702 passed / 1 skipped)

## 4. 执行大纲

1. 按 2.1 映射激活特性(环境变量 / ae.toml)
2. 按 `commands/dev-loop.md` 协议驱动 v5.6 tick 循环:`ae dev-loop --init "需求" --design-doc design/V1.0-Design-VoiceClonePage.md` → spawn subagent 执行 stage → `ae dev-loop --tick --result <file>` → 循环至 done
3. **发现 loop 问题 → 修 Auto-engineering 代码(TDD + 回归测试)→ 重试**,直至需求以生产级质量完成
4. 跑完:汇总 prompt 单文件 → 分析日志(_scratch/debug/ + metrics events.jsonl + audit log + offload/)→ 核验 T-task 设计符合性 → 完整报告
5. 启动 voice_clone 服务(**启动服务红线**:lsof 端口检查 → 杀残留 → 确认释放 → 启动),待用户验证
6. voice_clone 目录不删除,等用户通知

## 5. T-task 核验清单(设计目标 vs 真跑实际)

| T-task | 主题(tracker 出处) | 验证证据来源 | 符合性 |
|--------|------------------|-------------|:---:|
| T50 | 5 层提示词模板(Phase 17) | per-tick prompt 日志 | 待填 |
| T51a | 提示词增强子项(Phase 32) | prompt 日志 | 待填 |
| T51b | code-reviewer 工具兼容(Phase 32,原 ❌ 不兼容) | critic stage 行为 | 待填 |
| T51c | design_items 补全 Fix A(Phase 35) | architect 输出 | 待填 |
| T51d | impl_files 注入 Fix B(Phase 35) | component_verifier 输入 | 待填 |
| T51e | auto-skip Fix C(Phase 35) | verifier skip 行为 | 待填 |
| T51f | prompt 日志增强(Phase 35) | prompt-log 完整性 | 待填 |
| T53 | Stage context offloading 执行存档(Phase 18,2026-07-25 定位演进:存档+读回 API,跨 stage 上下文由 DS-15+T54 提供) | `.ae-state/offload/` 文件 | 待填 |
| T54 | SessionSummarizer 跨 tick 摘要注入(Phase 18) | prompt 中 session_summary 字段 | 待填 |
| T55 | Ollama adapter(Phase 18) | 本次未启用 Ollama,标"未覆盖" | 待填 |

## 6. 交付物清单

- `_scratch/test-output/<日期>-真跑报告-<后缀>.md` — 完整报告(T-task 符合性 + 问题汇总 + loop 修复记录)
- `_scratch/test-output/all-prompts-<时间戳>.md` — 全部提示词(待用户审核)
- voice_clone 开发成果 — 保留目录 + 服务运行中,等用户验证后通知删除

## 7. 红线与纪律

- voice_clone 目录删除:**等用户通知**(红线 #1,不擅自删)
- 服务启动:lsof 端口检查纪律(CLAUDE.md 启动红线)
- loop 修复:TDD + 回归测试守护;设计文档不可降级(design-document-inviolability)
- 前台反馈:每 tick/修复进度显式输出,不静默(CLAUDE.md atdo 前台纪律)
