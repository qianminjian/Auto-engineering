# v5.8 再次深度审计报告

日期：2026-08-02
候选版本：5.8.0-rc.4
审计提交：570e7ed

## 结论

自动门禁已满足受控真实运行的前置条件，但不能据此宣称 v5.8 已正式发布。
T336、T350、T367、T376 的真实 Claude Code/Codex LLM 轨迹仍是 P0 发布门禁。

## 自动证据

- 全量测试：2166 passed、1 skipped；覆盖率 90.41%。
- Ruff、mypy（133 个源码文件）、规则同步、元数据和 `make check-gate` 通过。
- 事故回归与核心专项：63 passed，包括 150 Tick/3 sessions、事件重放、Result 幂等、事务回滚、ProjectProfile 和成本约束。
- 当前提交 GitHub CI 通过，Release archive 构建成功。
- Claude Code/Codex archive smoke 均通过；验收报告明确 `product_install=not_run`。
- 以 `git ls-files -z | gitleaks stdin --redact=100` 扫描当前受版本控制文件：无发现。直接扫描工作目录会把被忽略的 `.ae-state/spawn-proofs`（约 295MB）当作输入并产生 103,293 条噪声，发布审计必须排除生成状态目录。完整 Git 历史扫描仍发现 15 条疑似秘密，须人工确认测试夹具和已删除历史 `src/index.html` 中的内容是否均为假数据或已撤销。

## 已实施的审计修复

- T377：`dev-loop --status --verbose` 批次摘要异常时使用显式空组件降级，不传播未初始化变量。
- T378：跨会话失败诊断在测试成功后清除对应计数；诊断不改变测试执行语义。

## 未完成或需人工确认

- 真实 Claude Code/Codex 产品安装与 LLM 轨迹尚未执行。
- `pip-audit` 本轮无法直接消费 `uv.lock`，未形成依赖漏洞“零发现”结论；该项仍是发布前人工/工具链确认项。
- 历史疑似秘密不能通过改写 Git 历史自动消除；须确认、撤销/轮换或形成误报记录。

## 真跑准入协议

1. 5-10 Tick：验证真实安装、`ae.toml`、runner、Action/Result、spawn proof、status/resume。
2. 30-50 Tick：注入一次中断和一次修复循环，验证恢复幂等、批次不回退、无人工交接。
3. 150 Tick：记录 input/cache/output、Core payload、重复块、事件投影、open findings、进度守恒和 compaction 可观测性。
4. Claude Code 与 Codex 使用同一需求和验收指标，比较最终事件投影与 verdict，不比较自然语言表面文本。

## 发布判定

只有两宿主真实产品安装为 `pass`，150 Tick 轨迹满足设计约束，历史秘密完成确认，
且依赖审计有可复现证据后，T321/T350/T367/T376 才能关闭并进入正式发布评审。
