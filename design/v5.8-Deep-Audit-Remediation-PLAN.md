# v5.8 深度审计修复计划

> 日期：2026-08-01｜状态：自动门禁完成，待真实产品轨迹｜来源：Phase 73 后深度审计

## 目标

关闭生产 Gate 断链、Result/Spawn fail-open、状态原子性与 EventStore/Host SPI
虚化、Profile 恢复漂移、Release/CI 和审计门禁失真问题。既有 D9、D15、D16
保持不变，设计与实现不一致时补齐实现。

## 任务与验收

| ID | 优先级 | 任务 | EARS 验收 |
|---|:---:|---|---|
| T368 | P0 | Profile Gate 生产执行链 | While Profile 声明精确命令, when Developer Gate 运行, the system shall 直接执行已解析 Gate 实例且不得按名称回退默认工具 |
| T369 | P0 | Result 强类型权威校验 | While Result 字段类型、范围或结构非法, when Tick 接收 Result, the system shall 在任何 Handler 前按 schema fail-closed |
| T370 | P0 | Spawn proof 因果绑定 | While Stage 要求 Worker, when Result 返回, the system shall 强制验证 active Action token、stage、thread、Action 和 Worker receipt |
| T371 | P0 | Result/事件/Action 原子提交 | While Result 被接受, when 持久化中任一点故障, the system shall 全部回滚或幂等返回同一响应 |
| T372 | P0 | EventStore 与 Host Adapter 产品接线 | While CLI init/tick/resume 运行, when 宿主交互 Core, the system shall 通过 Host Adapter 并以 EventStore 为新线程事实源，旧 checkpoint 仅兼容迁移 |
| T373 | P1 | Profile 恢复与领域事件 | While 本地证据在 checkpoint 后变化, when restore 运行, the system shall 重新解析 Profile、记录 resolved/conflict/setup 事件并重新校验 Gate |
| T374 | P1 | Release、依赖与跨平台加固 | While release 被触发, when 发布制品, the workflow shall 依赖完整质量门禁、locked 依赖、最小权限与受支持平台矩阵 |
| T375 | P1 | 审计门禁与验收去虚化 | While audit/release smoke 运行, when 发现违规, the command shall 非零退出且无 manifest、源码回链或产品验收冒充 |
| T376 | P0 | Phase 74 收口 | While T368-T375 完成, when 全量、覆盖率、Ruff、mypy、sync 与双宿主 archive 运行, all automatic gates shall 通过；真实 LLM 轨迹仍单独报告 |

## 实施顺序

1. T368-T370：先关闭可直接造成错误通过的入口。
2. T371-T373：统一事实源、事务和恢复语义。
3. T374-T375：修复发布及治理门禁。
4. T376：串行执行完整验证并更新基线。

## 非目标

- 不在 Core 内调用 LLM。
- 不把 archive smoke 表述为真实产品验收。
- 不修改外部参考源码，不新增全局依赖。
