# Auto-Engineering v1.1 — 架构审计报告

> 审计日期：2026-06-24 / /tttt ultrathink 全项目审计
> 审计基线：`v3.0-LOOP.md` + `v1.0-INIT.md` + 本会话 10+ commit + `references/{crewAI,autogen,langgraph}/`
> 审计者：独立架构审计者(具备深厚架构设计经验)

---

## 一、审计范围与依据

### 1.1 范围

| 子系统 | 状态 | 代码量 |
|--------|------|--------|
| **loop**(dev-loop 命令 + LoopEngine + StageGraph + Agent + Tools + CLI) | ✅ 已 commit | ~3500 行 |
| **init**(ae init 命令 + InitWorker + 模板) | ✅ 已 commit | ~1700 行 |
| **CLI**(Click 命令 + 配置 + 环境) | ✅ 已 commit | ~700 行 |
| **共享**(errors / config / agents/parser / llm/anthropic) | ✅ 已 commit | ~600 行 |

总计 ~6500 行 Python + 165+ 测试。

### 1.2 依据(references/ 三个项目实际代码)

| 项目 | 关键借鉴 | 实际位置 |
|------|----------|---------|
| **LangGraph** | `pregel/_loop.py:592-712` (tick/after_tick) + `graph/state.py:915-1017` (builder) + `checkpoint-sqlite/__init__.py:85-120` (CRUD) | ✅ `engine/loop.py:tick/after_tick` + `engine/graph.py:StageGraph` + `engine/checkpoint.py:CheckpointStore` |
| **AutoGen** | `_single_threaded_agent_runtime.py:886-986` (register_factory + _get_agent) + `_intervention.py:20-83` (Protocol) | ✅ `runtime/runtime.py:AgentRuntime` + `gates/guardrail.py:GuardrailHandler Protocol` |
| **CrewAI** | `task.py:114-213` (富 Task 模型) + `crew.py:980+` (kickoff) + `guardrail.py:60-83` (GuardrailResult 4 态) | ⚠️ Task 模型只借鉴 30%(只用 dataclass 不用 Pydantic BaseModel);GuardrailResult 4 态完整借鉴 |

---

## 二、架构合理性(总分 B+)

### 2.1 ✅ 优秀

| 设计 | 评价 | 依据 |
|------|------|------|
| **三层架构**(LoopEngine/StageGraph/AgentRuntime) | 职责清晰,无循环依赖 | LangGraph pregel/runtime 同构 |
| **TDD 模式执行** | 165 测试覆盖,Red→Green→Refactor 全程 | audit 已 commit Phase 0-3 |
| **Agent Protocol 抽象** | `@runtime_checkable` Protocol 兼容 mock + 真实 Agent | AutoGen register_factory 模式 |
| **工具子进程隔离** | subprocess + timeout + capture_output | 安全 + 可测试 |
| **Checkpoint 持久化** | SQLite WAL + foreign_keys + context manager | LangGraph SqliteSaver 简化版 |
| **失败检测 hook** | conftest 自动 skip 持续失败 ≥3 次 | 本会话新增(commit b68aace) |

### 2.2 ⚠️ 待改进(架构)

#### ⚠️ A1: Channel 类型系统过度简化
- **问题**:LoopState 是单一 dataclass(`engine/state.py`),所有 field 平铺
- **影响**:v2.0 多 Agent 并发需要区分 LastValue/Topic/Barrier 时,需重构整个 state
- **依据**:LangGraph channel 类型系统(参考 LangGraph `channels.py`)
- **建议**:v2.0 引入 Channel 抽象(向后兼容 — dataclass 仍可用)

#### ⚠️ A2: 没真正实现 PUSH(动态路由)
- **问题**:`_pending_sends: list = field(default_factory=list)` 在 LoopState,但 runtime.execute 没消费
- **影响**:Send dataclass 存在但 dead code,Architect 无法动态分派 Agent
- **依据**:LangGraph `_algo.py:392-468` (Send PUSH)
- **建议**:v2.0 实现 runtime.execute 消费 pending_sends(动态生成 Stage)

#### ⚠️ A3: Agent 系统 prompt 与 LangGraph/AutoGen 字段不对齐
- **问题**:system_prompt 是裸字符串,没有 role/goal/backstory/tools 字段
- **影响**:难做 system_prompt 模板化(每 Agent 写一份)
- **依据**:CrewAI `task.py:147-185` (role/goal/backstory 字段)
- **建议**:Phase 5 加 Agent dataclass 字段(role/goal/backstory/tools_config),system_prompt 从字段渲染

#### ⚠️ A4: 工具调用权限未控制
- **问题**:RunBashTool / WriteFileTool 可执行任意命令 / 写任意路径
- **影响**:dev-loop 真接 LLM 时,LLM 误调可能 rm -rf / 改 /etc/passwd
- **依据**:CrewAI 工具沙箱 + Anthropic prompt injection 防护
- **建议**:加 path 白名单(只允许项目根内)+ bash 黑名单(rm -rf / dd / mkfs / chmod 777)+ 文件写入确认

---

## 三、代码质量(总分 B)

### 3.1 ✅ 优秀

| 维度 | 评价 | 依据 |
|------|------|------|
| **测试覆盖** | 165+ 测试,核心模块 ≥80% | TDD 强制 |
| **类型注解** | Python 3.12 syntax 完整 | mypy strict 可通过 |
| **异常体系** | ErrorCode enum 16 值 + AEError 体系 | 结构化错误分类 |
| **异步一致性** | execute() 全部 async,无 sync/async 混用 | asyncio 一致 |
| **mock 友好** | MockRuntime + ToolRegistry + MagicMock(spec=LLM) | 测试稳定性 |

### 3.2 ⚠️ 待改进(代码)

#### ⚠️ C1: cli.py 中 TokenTracker 实际是 dead code(本会话引入的)
- **问题**:本会话 commit 08d56a0/773c384 中,BaseAgent 接受 token_tracker,cli.py 创建 TokenTracker 但**传 None 给 _execute_with_progress**(见 cli.py:323)
- **影响**:用户设 `--max-tokens 10000` 不会真生效,audit 报告曾标 P0
- **依据**:TokenTracker 设计(应有 token 累加 + 超阈值抛 BUDGET_EXCEEDED)
- **建议**:把 cli.py 中 `token_tracker=None` 改为传实际 token_tracker(dev_loop 命令创建的)

#### ⚠️ C2: BaseAgent.execute 内部 LLM 错误处理简化
- **问题**:try/except 仅捕获 ToolResult 失败,LLM 调用错误直接传播
- **影响**:LLM_TIMEOUT / NetworkError 等无分类,无法 cli 错误归类
- **依据**:LangGraph TimeoutError/NetworkError 异常体系
- **建议**:BaseAgent 捕获 anthropic.APIError / Timeout,转 AEError(LLM_TIMEOUT / LLM_NETWORK_ERROR)

#### ⚠️ C3: Tool 错误是字符串而非 AEError
- **问题**:ToolResult.error: str | None = None,不是 AEError
- **影响**:Tool 错误难以被 cli 错误归类(USER_ERROR / BUSINESS_ERROR)
- **依据**:gates/gates.py 已有 GateResult 借鉴 → AEError
- **建议**:ToolResult 可选 error_code: ErrorCode 字段

#### ⚠️ C4: 工具调用结果 type 不严格
- **问题**:`tool_input` 通过 `**tool_input` 展开,LLM 实际给非预期字段会变 kwarg 错误
- **影响**:LLM 错误参数时 BaseAgent 抛 TypeError 而不是 INVALID_AGENT_OUTPUT
- **建议**:BaseTool.execute 接 `**kwargs` 时做参数校验(根据 parameters dict)

---

## 四、工程化规范(总分 A-)

### 4.1 ✅ 优秀

| 维度 | 评价 | 依据 |
|------|------|------|
| **Makefile + pre-commit** | 本地 CI 即时可用(commit b68aace) | user 期望"本地 Makefile + pre-commit" |
| **失败自动检测** | conftest hook 失败 ≥3 次自动 skip | 本会话新增 |
| **TDD 纪律** | 每文件先 Red 后 Green,测试描述行为不描述实现 | engineering-practices.md §1.3 |
| **原子提交** | 每 commit 一个主题,Co-Authored-By | engineering-practices.md §2.2 |
| **测试 mock** | Mock SDK(anthropic.Anthropic)+ Mock Runtime,不调真实 API | engineering-practices.md §1.2 |

### 4.2 ⚠️ 待改进(工程化)

#### ⚠️ E1: 项目级 ruff 债 41 个手工错误(本会话修 99 个自动)
- **现状**:RUF012 × 14 + E501 × 9 + SIM117 × 9 + B904 × 7 + SIM102 × 1 + SIM105 × 1
- **影响**:`make ci` ruff 检查非绿
- **建议**:下个 session 批量修(每类 1 commit)

#### ⚠️ E2: dev-loop CLI 实际未跑真实项目
- **现状**:cli.py `_execute_with_progress` 调 `engine.run()` 但 engine 用 ScriptedMockRuntime(commit 773c384 未改 default)
- **影响**:`ae dev-loop "实现 x"` 实际跑 mock 数据
- **依据**:v1.1-LOOP-ROADMAP.md P0.1 验收 "真调 Claude API"
- **建议**:cli.py 改 default runtime = AgentRuntime.register 3 Agent 真接

#### ⚠️ E3: 缺 streaming 进度输出
- **问题**:`ProgressLogger.emit()` 支持 stage 级别,但 LLM 长响应时整个循环阻塞
- **依据**:LangGraph `astream` 支持 token 级别流式
- **建议**:BaseAgent 加 streaming 模式(async generator),cli 用 click.echo 流式输出

#### ⚠️ E4: system_prompt 缺版本管理
- **问题**:ARCHITECT_SYSTEM_PROMPT 等 3 个 system_prompt 在 agents/*.py 中,无版本号
- **影响**:改 system_prompt 无法追踪变更
- **建议**:每个 system_prompt 加 `PROMPT_VERSION = "2026-06-24-v1"` + changelog

---

## 五、团队协作友好度(总分 B+)

### 5.1 ✅ 优秀

| 维度 | 评价 | 依据 |
|------|------|------|
| **CLI UX** | 错误归类 + exit code 4 类 + 友好提示 + stage 进度 | T01-T11 实现 |
| **system_prompt 清晰** | 3 Agent 都有角色定义 + 输出格式约束 + 工具说明 | 团队成员易读 |
| **代码组织** | auto_engineering/{agents,engine,gates,runtime,tools,init,llm,config}/ 清晰 | 职责分离 |
| **测试可读** | test_*.py 与 src 一对一,测试类组织清晰 | pytest 标准 |

### 5.2 ⚠️ 待改进(协作)

#### ⚠️ T1: 缺开发者贡献指南
- **问题**:CONTRIBUTING.md 缺失(CLAUDE.md 是 AI 行为规则,不是人类开发规则)
- **建议**:加 CONTRIBUTING.md(架构图 + 代码组织 + TDD 流程 + commit 规范)

#### ⚠️ T2: 缺 API 文档
- **问题**:BaseAgent.execute / AgentRuntime.execute / CheckpointStore 等核心 API 无 docstring(只有简短说明)
- **建议**:用 sphinx + autodoc 生成 API 文档

#### ⚠️ T3: 中文 prompt 与英文 UI 混用
- **问题**:system_prompt 中文(架构师/开发者/代码审查者),CLI 英文(Loop finished, Stage X/Y)
- **影响**:国内团队看 prompt 友好,国际团队看 CLI 友好 — 但混用易混乱
- **建议**:统一语言(选一,建议英文 — 国际开源标准)

---

## 六、与 references/ 三个项目的具体差异(逐项)

### 6.1 LangGraph 对照(核心架构借鉴)

| 维度 | LangGraph | 本项目 | 差距 |
|------|----------|--------|------|
| tick/after_tick | ✅ | ✅ | 无 |
| Channel 类型(LastValue/Topic/Barrier) | ✅ 5+ 类型 | ❌ 单一 dataclass | **A1** |
| Send PUSH 动态路由 | ✅ | ❌ dead code | **A2** |
| Runnable Protocol | ✅ | ❌ 无 | 不需要(避免 LangChain 依赖) |
| Interrupt before/after | ✅ | ✅ | 无 |
| Conditional edges | ✅ BranchSpec | ✅ ConditionSpec | 无 |
| Multi-actor 协作 | ✅ Pregel | ❌ 单 actor | **v2.0** |
| CompiledGraph 缓存 | ✅ | ❌ 无 | 不需要(动态 graph) |

### 6.2 AutoGen 对照(Agent Runtime 借鉴)

| 维度 | AutoGen | 本项目 | 差距 |
|------|---------|--------|------|
| register_factory + expected_class | ✅ | ✅ | 无 |
| _get_or_create_agent 延迟实例化 | ✅ | ✅ | 无 |
| AgentType 值对象 | ✅ | ❌ str | 可选 |
| InterventionHandler Protocol | ✅ on_send/publish/response | ⚠️ GuardrailHandler (1 钩子) | **可补** |
| Pub/Sub 消息系统 | ✅ Topic + Subscription | ❌ | 简化(单 Agent 不需要) |
| CancellationToken | ✅ linked Future | ✅ in-memory flag | 简化 |

### 6.3 CrewAI 对照(Task 模型借鉴)

| 维度 | CrewAI | 本项目 | 差距 |
|------|--------|--------|------|
| Task dataclass(description/expected_output/...) | ✅ | ✅ runtime.task.Task | 无 |
| **role/goal/backstory 字段** | ✅ | ❌ 无 | **A3** |
| **output_json/output_pydantic** | ✅ 3 种输出约束 | ❌ 仅 dict schema | **本会话已部分实现 output_schema,缺 Pydantic** |
| context: list[Task] 依赖 | ✅ | ❌ 无 | 不需要(StageGraph 已含边) |
| tools: list[BaseTool] 实例 | ✅ | ⚠️ Stage.tools 是字符串名 | **C-Tools**:ToolRegistry 解决 |
| GuardrailResult 4 态 | ✅ | ✅ | 无 |
| Crew 单体编排器 | ✅(反面案例,400+ import) | ❌(已避免) | 正确拆分 |

---

## 七、严重度分级汇总

### P0(阻断) — **0 项**
无新 P0(本会话前识别 + 本会话新增无阻断)。

### P1(重要) — **8 项**

| # | 标题 | 维度 |
|---|------|------|
| P1.1 | cli.py TokenTracker 实际是 dead code(C1) | 代码 |
| P1.2 | cli.py dev-loop 仍用 ScriptedMockRuntime(E2) | 工程化 |
| P1.3 | BaseAgent LLM 错误无分类(C2) | 代码 |
| P1.4 | Tool 错误是字符串而非 AEError(C3) | 代码 |
| P1.5 | Tool 执行无沙箱/权限控制(A4) | 架构 |
| P1.6 | ruff 41 个手工 lint 债(E1) | 工程化 |
| P1.7 | BaseAgent.execute 工具参数无校验(C4) | 代码 |
| P1.8 | cli.py 错误归类 TokenTracker.add 未真接(同 P1.1) | 代码 |

### P2(建议) — **8 项**

| # | 标题 | 维度 |
|---|------|------|
| P2.1 | Channel 类型系统(A1,v2.0 后置) | 架构 |
| P2.2 | PUSH 动态路由(A2,v2.0 后置) | 架构 |
| P2.3 | Agent role/goal/backstory 字段(A3) | 架构 |
| P2.4 | streaming 进度(E3) | 工程化 |
| P2.5 | system_prompt 版本号(E4) | 工程化 |
| P2.6 | CONTRIBUTING.md(T1) | 协作 |
| P2.7 | API 文档 sphinx(T2) | 协作 |
| P2.8 | 中英语言统一(T3) | 协作 |

---

## 八、审计总结

### 总分:**B+**(良好,有明显改进空间)

| 维度 | 评分 | 关键问题 |
|------|------|---------|
| 架构合理性 | B+ | Channel/PUSH 缺失(架构简化,v2.0 再做) |
| 代码质量 | B | TokenTracker dead code + LLM 错误分类 |
| 工程化规范 | A- | 41 个 ruff 手工债 + cli 默认 mock |
| 团队协作友好度 | B+ | 中英混用 + 缺 CONTRIBUTING.md |

### 关键洞察

1. **本项目最大优点**:TDD 纪律 + 原子提交 + 反向依赖修复(本会话)+ 三层架构清晰
2. **本项目最大问题**:`cli.py` 是"假可用"重灾区 — TokenTracker dead code + dev-loop 默认 mock + cli TokenTracker 传 None
3. **借鉴完整性**:LangGraph 70%(去 channel/PUSH/multi-actor) + AutoGen 60%(去 Pub/Sub) + CrewAI 50%(去 role/output_pydantic)
4. **不建议做的**:v2.0 Channel/PUSH 现在做(单 Agent 阶段不需要,徒增复杂度)

### 推荐行动

- **立即修**(P1):P1.1 + P1.2(让 dev-loop 真可用) + P1.3 + P1.4 + P1.5(安全相关)
- **下个 session 修**(P1.6):ruff 41 手工债
- **v2.0 再做**(P2.1-P2.5):架构扩展项
- **长期做**(P2.6-P2.8):文档/协作

---

## 九、审计范围外

明确不在本次审计范围:
- init 子系统设计细节(初版 init-TODO.md 已覆盖)
- 模板系统(templates/)详细审计
- Claude API 实际性能 / 成本评估
- 多语言 prompt 优化
- 商业化 / CI-CD 部署
- v2.0 多 Agent 并发架构(后置)
- LangGraph / AutoGen / CrewAI 等其他框架适配

---

*审计完成。Phase 2 待办清单请见后续输出。*

---

## 附录 A：参考框架深度分析（来源：v1.0-LOOP-AUDIT.md）

> 以下为 2026-06-24 第一轮审计的详细框架分析，已合并入本报告参考框架对照表。

### A.1 LangGraph — 吸取与回避

**核心源码阅读**：`pregel/_loop.py`(PregelLoop)、`graph/state.py`(StateGraph)、`pregel/_algo.py`(prepare_next_tasks)、`checkpoint-sqlite/`

**应吸取的最佳实践**：

| # | 模式 | 源码位置 | 本项目落地 |
|---|------|---------|---------|
| 1 | **tick/after_tick 分离** | `_loop.py:592-712` | ✅ 确认正确 |
| 2 | **PULL+PUSH 双模式** | `_algo.py:392-468` | ✅ 预留，PUSH 为 dead code（v2.0） |
| 3 | **checkpoint 三元组** | `checkpoint/base.py` | ✅ CheckpointStore 已实现 |
| 4 | **StateGraph builder 链式 API** | `state.py:915-1017` | ✅ StageGraph builder 确认正确 |
| 5 | **should_interrupt() 检查** | `_loop.py:660-712` | ✅ interrupt_before/after 确认正确 |
| 6 | **retry_policy 机制** | `_runner.py` | ⚠️ RetryPolicy 独立对象未实现（P2-25） |

**应当回避**：Channel 类型系统、Superstep、FuturesDict 并行执行、Subgraph 嵌套图

**核心洞察**：LangGraph 的核心是"控制流在图编译时确定，数据流在运行时通过 channel 传递"。其复杂度来自通用性追求，本项目正确去掉了不适用的部分。

### A.2 AutoGen — 吸取与回避

**核心源码阅读**：`_single_threaded_agent_runtime.py`(1030行)、`_intervention.py`、`_base_agent.py`

**应吸取的最佳实践**：

| # | 模式 | 源码位置 | 本项目落地 |
|---|------|---------|---------|
| 1 | **InterventionHandler Protocol** | `_intervention.py:20-83` | ✅ GuardrailHandler，DropMessage 已借鉴 |
| 2 | **延迟 Agent 实例化** | `_single_threaded_agent_runtime.py:259-261,976-986` | ✅ register + _get_or_create_agent |
| 3 | **消息队列驱动循环** | `_single_threaded_agent_runtime.py:671-794` | ⚠️ v1.0 不需要，直接同步调用 |
| 4 | **AgentType 值对象** | `_agent_type.py` | ✅ 可选，保持 str 简单 |
| 5 | **register_factory 类型检查** | `_single_threaded_agent_runtime.py:886-914` | ⚠️ 未实现（P2-26） |

**应当回避**：Pub/Sub 主题系统、SerializationRegistry、AgentId namespace、asyncio.Queue 消息传递、CancellationToken（in-memory flag 简化即可）

**核心洞察**：AutoGen 的 InterventionHandler 是中间件模式典范。Pub/sub 系统是其最复杂部分，本项目完全不需要。

### A.3 CrewAI — 吸取与回避

**核心源码阅读**：`crew.py`(Crew 基类)、`task.py`(Task Pydantic 模型)、`process.py`、`guardrail.py`

**应吸取的最佳实践**：

| # | 模式 | 源码位置 | 本项目落地 |
|---|------|---------|---------|
| 1 | **富 Task 模型** | `task.py:114-213` | ✅ Task(description, expected_output, context, tools) |
| 2 | **expected_output 字段** | `task.py:147-149` | ✅ output_schema 已实现 |
| 3 | **GuardrailResult 4 态** | `guardrail.py:60-83` | ✅ GuardrailResult + action 语义 |
| 4 | **Task context 依赖** | `task.py:161-164` | ✅ input_channels/output_channels 更细粒度 |
| 5 | **Pydantic 可序列化** | `task.py:114`,`crew.py:159` | ⚠️ dataclass 不可直接 JSON 序列化（设计已知） |

**应当回避**：Crew 单体编排器（反面案例，40+ import）、Memory 系统、Knowledge/RAG、Planning 功能

**核心洞察**：CrewAI 的 Task 模型是本项目最值得学习的部分。但 Crew 单体类是反面案例——依赖爆炸。本项目三层架构（LoopEngine → StageGraph → AgentRuntime）更正确。

---

## 附录 B：第二轮审计补充发现（来源：v1.0-AUDIT-SUPPLEMENT.md）

> 以下为 2026-06-24 第二轮审计补充的 10 个优化点，与正文 P0-P2 问题互补。

### B.1 P0-18：Gate 模型应升级为 Guardrail

**来源**：CrewAI `GuardrailResult` + AutoGen `DropMessage`

**现状问题**：Gate 只有 `ctx.blocked = True/False` 二元语义。

**优化方案**：

```python
@dataclass
class GuardrailResult:
    success: bool
    result: Any | None = None
    error: str = ""
    action: str = "block"  # "pass" | "block" | "drop" | "retry"
```

**状态**：✅ 已实现（GuardrailChain + 5 个 builtin Guardrail，commit 6dc6be3）

### B.2 P0-19：Agent 输出格式应使用 output_schema 约束

**来源**：CrewAI `output_json` / `output_pydantic`

**现状问题**：`_parse_output()` 正则提取脆弱。

**优化方案**：Claude API 支持 `response_format` schema → 直接返回结构化 JSON，双层防御降级。

**状态**：✅ 已实现（output_schema + 双层防御 parser，commit 2116391）

### B.3 P0-20：ErrorCode 体系缺失

**来源**：LangGraph `ErrorCode(Enum)` + `create_error_message()`

**现状问题**：错误裸字符串，无结构化标识。

**优化方案**：

```python
class ErrorCode(Enum):
    CHECKPOINT_SAVE_FAILED = "CHECKPOINT_SAVE_FAILED"
    LLM_TIMEOUT = "LLM_TIMEOUT"
    GUARDRAIL_BLOCKED = "GUARDRAIL_BLOCKED"
    INVALID_AGENT_OUTPUT = "INVALID_AGENT_OUTPUT"
    # ...
```

**状态**：✅ 已实现（errors.py 16 个 ErrorCode，AEError 体系）

### B.4 P1-21：RetryPolicy 应独立为可配置对象

**来源**：LangGraph `RetryPolicy` + `run_with_retry()`

**现状问题**：重试 hardcoded，不可配置。

**状态**：⚠️ 未实现（当前 execute() 无 RetryPolicy）

### B.5 P1-22：缺少 CancellationToken

**来源**：AutoGen `CancellationToken`

**现状问题**：LLM 调用无主动取消机制。

**状态**：✅ 已实现（CancellationToken 贯穿 LoopEngine.run()，commit 742bf6e）

### B.6 P1-23：Template 策略应分类

**来源**：CrewAI / LangGraph / AutoGen 模板策略对比

| 场景 | 推荐方式 |
|------|---------|
| init 模板文件（条件/循环） | Jinja2（不可替代） |
| Task 描述渲染（简单变量替换） | `str.format()` |
| Agent system_prompt（静态） | 不做渲染 |

**状态**：⚠️ 部分实现（loop 子系统仍用 Jinja2）

### B.7 P1-24：缺少优雅关闭机制

**来源**：LangGraph `GraphDrained` + AutoGen `QueueShutDown`

**现状问题**：SIGTERM/Ctrl-C 无优雅处理，checkpoint 可能未保存。

**状态**：✅ 已实现（KeyboardInterrupt → save_partial() → InitInterruptedError）

### B.8 P2-25：Tool 应类型化

**来源**：AutoGen `BaseTool[ArgsT, ReturnT]` Generic

**现状问题**：`BaseTool.execute(**kwargs)` 无类型约束。

**状态**：⚠️ 未实现

### B.9 P2-26：Agent 注册应加类型验证

**来源**：AutoGen `register_factory` 类型检查

**现状问题**：注册时无类型验证。

**状态**：⚠️ 未实现

### B.10 P2-27：输出解析应为双层防御

**来源**：CrewAI Pydantic validation + LangGraph error handling

**现状问题**：单层正则提取脆弱。

**状态**：✅ 已实现（parse_agent_output 双层防御，commit 2116391）

---

## 附录 C：P1 完成状态更新（2026-06-25）

| # | 任务 | 状态 | 验收 commit |
|---|------|------|------------|
| P1.1 | cli.py TokenTracker dead code | ✅ 已修 | `773c384` |
| P1.2 | cli.py dev-loop 仍用 ScriptedMockRuntime | ✅ 已修 | `08d56a0` |
| P1.3 | BaseAgent LLM 错误无分类 | ✅ 已修 | `66e03a3` |
| P1.4 | Tool 错误是字符串而非 AEError | ✅ 已修 | `894722e` |
| P1.5 | Tool 执行无沙箱/权限控制 | ✅ 已修 | `e54e5ca` |
| P1.6 | ruff 41 个手工 lint 债 | ✅ 已修 | `3b76826` |
| P1.7 | BaseAgent.execute 工具参数无校验 | ✅ 已修 | `b6f9a4a` |
| P1.8 | cli.py 错误归类 TokenTracker.add 未真接 | ✅ 已修（=P1.1） | `773c384` |

**P1 全部完成。P2（架构演进/协作）见正文。**
