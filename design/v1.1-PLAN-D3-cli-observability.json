{
  "version": "1.0",
  "goal": "实现 dev-loop Phase 4: 端到端 CLI (dev-loop/status/checkpoint list/resume) + 可观测性 (结构化日志 + token 追踪) + RetryPolicy + CancellationToken + Settings 接入 + 最终集成 Gate。",
  "phases": [
    {
      "name": "D3.01: engine/retry.py + cancellation.py + logging.py + cli.py 完整接 LoopEngine",
      "goal": "实现 engine/retry.py (RetryPolicy + run_with_retry) + engine/cancellation.py (CancellationToken) + engine/logging.py (结构化日志 + token 追踪)；cli.py dev-loop 端到端接通。",
      "tasks": [
        "建 engine/retry.py: RetryPolicy dataclass (max_attempts/initial_delay/backoff_factor/max_delay/retry_on/timeout) + run_with_retry 异步函数",
        "建 engine/cancellation.py: CancellationToken 类 (cancel/is_cancelled/check) — v3.0 §7.2",
        "建 engine/logging.py: 结构化 JSON 日志 + TokenTracker (累加 LLMUsage) + StageLogger (stage/elapsed/tokens)",
        "更新 engine/loop.py LoopEngine.run() 接入 CancellationToken (Ctrl-C 时 cancel → 保存 checkpoint → 抛 TASK_CANCELLED)",
        "更新 engine/loop.py run() 退避改用 RetryPolicy 参数化 (替代硬编码 2**retry_count) — 修 v3.0 D11",
        "更新 cli.py dev_loop 端到端：preflight → load settings → 读 .ae-answers.yml → build graph + runtime + guardrail → engine.run() → 进度输出 → 退出码",
        "cli.py status 命令读最新 checkpoint 输出 (step/stage/status)",
        "cli.py checkpoint list 扫描 .ae-checkpoints/ 输出所有 thread",
        "cli.py checkpoint resume <id> 调 engine.resume(id)",
        "加 12 测试：RetryPolicy 退避/CancellationToken cancel/TokenTracker 累加/CLI 端到端 mock",
        "Gate: 端到端 ae dev-loop '简单需求' 在测试项目跑通（用 mock LLM）",
        "git commit feat(cli,engine): RetryPolicy+CancellationToken+结构化日志+CLI 端到端 (Plan D3)"
      ],
      "depends_on": [],
      "is_gate": false,
      "gate_type": "auto",
      "phase_type": "implementation"
    },
    {
      "name": "D3.02: 端到端集成 Gate — 5 团队成员试用 + 真实 LLM 跑通",
      "goal": "v1.0 完整端到端验证：5 个内部项目用 ae dev-loop 跑真实需求（mock LLM 可，真实 LLM 需 API key）；Gate 通过后更新 BEACON.md 标记 v1.0 内部发布。",
      "tasks": [
        "选 1 个真实内部项目（如本 Auto-Engineering 自身）做 dev-loop 端到端 dry-run → 确认 plan 输出符合预期",
        "选 1 个真实需求（如'实现本文件 CLAUDE.md 工具函数'）做真实 LLM 调用 → 确认 3 Stage 闭环 + git commit",
        "收集 3 个团队成员的试用反馈 → 修复 UX 问题",
        "更新 README.md 列出 ae init/ae dev-loop/ae status/ae checkpoint 命令",
        "更新 BEACON.md：阶段='v1.0 内部发布就绪'，下一步='v1.0-rc1 公开灰度'",
        "更新 CLAUDE.md 加入 ae 命令工作流",
        "跑全测试 77+25+6+12+新 = ~130 测试全绿",
        "最终 Gate: pytest tests/ -q 全绿 + 端到端 manual review",
        "git commit chore(release): v1.0 内部发布就绪 (Plan D3)"
      ],
      "depends_on": ["01"],
      "is_gate": true,
      "gate_type": "manual",
      "phase_type": "implementation"
    }
  ]
}
