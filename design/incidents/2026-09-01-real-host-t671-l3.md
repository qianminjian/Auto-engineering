# 2026-09-01 T671 同 Build 真实宿主 L3 记录

## 结论

候选 Build `5.8.0-rc.5+sha256.3d1dd83ed23463ea` 已完成双宿主串行安装、archive smoke，
并完成一次 Codex 真实 L3。Codex 从一次 `$auto-engineering` 入口经过
`project_setup → gap_scan → architect → developer → critic`，第 7 Tick 到达
`GOAL_ACHIEVED / TERMINAL`。Core 同时明确标记 `product_business_acceptance` 未验证，
因此这不是 L4 或发布通过证据。

## Codex

- 项目：`/private/tmp/ae-a008-codex-l3-t671-20260901`
- 入口：`codex exec --ephemeral ... '$auto-engineering ...'`
- 轨迹：setup 3 Tick（包含项目工具补齐）、gap scan、Architect、Developer、Critic。
- Architect/Developer/Critic 均由宿主原生 Worker 执行；Worker outcome、attestation、receipt
  和 Finalizer 链路均由宿主/Core 产生，未手工写入协议事实。
- 结果：Critic `APPROVE`，`3/3` 项目测试通过，Core Tick 7 返回 `TERMINAL`；设计文件未修改，
  Architect `file_targets` 使用项目根相对 POSIX 路径。
- 观察项：本机 Codex models cache 持续报告缺少 `base_instructions`；一次过时的
  `--close-worker` 命令被 Runner 拒绝，随后原生 `close_agent` 正常完成，未影响终态。

## Claude Code

- 项目：`/private/tmp/ae-a008-claude-l3-t671-20260901`
- 入口：`claude -p ... '/auto-engineering:dev-loop ...'`
- 轨迹：setup 2 Tick、gap scan、Architect Action；setup Gate 已收敛，设计文件 hash 未变，
  只读与相对路径合同已进入真实 Action。
- Architect 请求在上游宿主等待超过 15 分钟，按执行规则正常停止；没有伪造 Worker、Result
  或手工修复 `.ae-state`。本次 Claude 证据为未完成的 L3，不得记为通过。

## 发布判断

T670-T671 的代码实现和回归已完成，Codex L3 提供了真实合同证据；Claude L3 未完成，双宿主
L4 尚未启动，A008 与 `v5.8.0-rc.5` 发布门禁继续保持阻断。

## Claude 有限重试

- 第二次全新会话项目：`/private/tmp/ae-a008-claude-l3-t671-retry-20260901`。
- 同一 Build 重试轨迹已推进至第 11 Tick 的 `plate_deep_audit`，覆盖 setup、gap scan、
  Architect、Developer、Critic、Component Verifier 回环；设计文件仍保持只读。
- 在上游审计请求超过 15 分钟没有新 Action 后正常终止。该证据仍属于宿主等待阻塞，不能
  视为 L3 通过；未执行手工协议修复或重复旧 Worker。

## Claude 可观测重试与新缺陷

- 第三次全新会话项目：`/private/tmp/ae-a008-claude-l3-t671-filtered-20260901`，仍使用同一
  Build。改用 `stream-json` 过滤观测后，确认宿主不是“无响应”：已连续推进到 Tick 11，完成
  `project_setup → gap_scan → architect → developer → critic → component_verifier`，并实际
  派生多个原生 Worker；设计 hash 未变，未手工修改 `.ae-state` 协议事实。
- 该次运行暴露了一个结果契约缺陷：B2 是 README 文档任务，Worker 正确判断现状已满足并返回
  `files_changed=[]`，但 Core 的 Developer Result 验证器强制至少 1 个文件，返回
  `HOST_EVIDENCE_INVALID / COORDINATOR_RESULT_INVALID: files_changed 至少 1 个文件`。
  宿主随后被迫把“负责且已验证的 README.md”填入 `files_changed` 才能继续，这不符合
  `files_changed` 只能记录真实变更的事实边界。
- 继续推进至 Tick 11 的 Component Verifier 时，Claude 上游 API 返回
  `400 The content[].thinking in the thinking mode must be passed back to the API`，该次运行
  因宿主错误终止，不能视为 L3 通过。该错误属于 Claude/上游会话协议问题，不是 Core Tick
  状态机或 Worker 业务结果失败。

## 当前判断

T672 已收口 Developer 提示的 batch 边界：当前 Action 明确只执行当前 task 的
`file_targets`，不得跨 batch 提前实现其他设计项，`files_changed` 只填写本次真实变更。
这修复了本次真实轨迹中 B1 提前创建 README、继而使 B2 变成空变更批次的诱因；但仍需在
同一 Build 上重新执行真实 Claude L3，并完成 Codex/Claude 双宿主 L4，A008 继续阻断。

## T674-T675 新鲜证据（2026-09-02）

- 新 Build `5.8.0-rc.5+sha256.21f6a2c3ae5c1494` 已完成 Codex/Claude archive smoke 和真实
  staging 安装；全量自动回归 `2763 passed, 1 skipped`，覆盖率 `90%`。
- Claude 在新 Build 的 Gap Scan 仍曾把已有明确设计条目的实现缺失判为
  `clarity=missing`，进入 `gap_review` 用户 Gate。T674 已在 Core 增加确定性校验：显式设计条目
  不得使用 `clarity=missing`，结果返回 `GAP_ANALYSIS_IMPLEMENTATION_MISCLASSIFIED`，不再把
  正常 Developer 工作升级为用户决策。
- T675 重跑真实空项目时，Setup 暴露 PEP 735 开发依赖组与宿主运行环境边界问题：模型使用
  `[project.optional-dependencies]` 后执行 `uv sync --dev`，导致项目 `.venv` 中的 pytest 等
  工具被同步移除；另将 `uv build` 的 sdist 误用于含 `.ae-state` 绝对 symlink 的项目目录，产生
  构建失败和重复试错。已主动中止本次运行，未手工修改协议事实；下一步是把
  `[dependency-groups]`、测试根、项目工具可执行性和 Setup 重试上限做成协议级约束。

因此新 Build 尚不能记为 Claude L3 通过，双宿主 L4 仍未启动，A008 与发布门禁继续阻断。

## T675 实现后复验（2026-09-02）

- Core 已新增 `project_setup_failure_streak` 事件投影：Setup Result 连续失败 3 次后发出
  `PROJECT_SETUP_RETRY_EXHAUSTED` / `WAIT_RESOURCE`，成功验证后清零；该路径有状态回放和公开
  协议回归覆盖。全量自动回归为 `2764 passed, 1 skipped`，覆盖率 `90%`。
- 最终候选 Build `5.8.0-rc.5+sha256.43829a589c4d6a91` 的 archive smoke 与 Codex/Claude 本地安装均通过。
  真实 Claude Setup 首次正确遵守 capability-only，并使用 PEP 735 `dependency-groups.dev`；
  但在同一 Action 内连续重复 `uv build`，仍因 `.ae-state/.ae-runtime/bin/python` 的绝对 symlink
  进入 sdist 而失败，随后尝试删除 `dist/` 再重建。该次已主动中止，未进入 Core 的三次失败计数，
  也未记为 Claude L3 通过。
- 因此当前补强同时覆盖 Core 跨 Tick 限流和宿主单 Action 内的“一次就地修复后必须提交 Result”
  约束；仍需新 Build 真实验证宿主遵守该边界，并完成双宿主 L4，A008 继续阻断。

## T676 实现后协议复验（2026-09-02）

- `project_setup` Result 新增 `project_setup_failed` 分支，要求稳定 `failure_code`、不超过 512
  字符的 `failure_summary` 和 `attempts_in_action`（1 或 2）；Action 同时下发机器可读的
  `setup_attempt_policy`。失败事实以 `ProjectSetupFailed` 写入 EventStore，再由 Core 生成下一
  setup Action；连续三次进入 `PROJECT_SETUP_RETRY_EXHAUSTED` / `WAIT_RESOURCE`。
- 公开 CLI 失败交接轨迹、重复失败熔断、成功后清零、Result/Action JSON Schema、EventStore
  回放和双宿主 archive smoke 均通过；全量自动回归 `2771 passed, 1 skipped`，覆盖率 `90%`。
- 候选 Build 为 `5.8.0-rc.5+sha256.629dec4b4977afe7`。本批尚未重新执行真实 Claude L3，因而
不能宣称宿主已经遵守单 Action 边界；Claude L3 与 Codex/Claude 双宿主 L4 仍是 A008 的发布阻断项。

## T677 Core 范围闸门（2026-09-02）

T676 的失败交接协议解决了“宿主是否把失败交回 Core”，但真实 Claude 复验又暴露更
底层的问题：宿主在收到 capability-only setup Action 后，仍可能先创建业务源码和业务
测试，之后再提交 setup 结果。仅靠 Prompt 禁令不能形成产品边界。

T677 将该边界下沉到 Core：init 记录排除运行态目录的项目文件基线；setup Result 声称
完成后，Core 比较新增文件并按 ProjectProfile 的 source/test roots 分类。新增业务源码或
普通业务测试生成 PROJECT_SETUP_SCOPE_VIOLATION，写入
ProjectSetupFailed(source=core_validation)，保持 setup 并进入既有失败计数；最小 smoke
文件和工程元数据仍可通过。新增 2 条范围回归与 EventStore 事实回放回归，定向
115 passed。

真实宿主结论保持不变：T676 的 Claude 运行已证明 Prompt-only 约束不足，T677 尚未在
同一新 Build 上重跑 Claude L3；因此不能宣称 Claude L3 或双宿主 L4 已通过。

## T678-T679 真实复验与核心修复（2026-09-02）

- T678 Claude 新 Build 在 Setup 完成后首个恢复 Tick 稳定暴露
  `STATE_PROJECTION_MISMATCH (project_profile, project_profile_id)`；同时确认宿主曾为让
  smoke 通过创建设计中的业务模块桩，Core 应拒绝该越界。
- 根因不是等待或 Worker 中断，而是 `TickKernel._owned_channels()`把一个只更新失败计数的
  `ProjectStateUpdated` 误判为拥有全部 project channels，遮蔽了 Profile fallback 事实。
  T679 已改为按 delta payload 实际字段计算 ownership；新增跨进程回归通过。
- 使用原真实 Claude fixture 的公开 `ae-run --tick` 重放后，状态已正确提交
  `ProjectStateUpdated(project_profile, project_profile_id, ...)`，并返回新的
  `project_setup_required` / `PROJECT_SETUP_SCOPE_VIOLATION`，不再返回投影漂移错误。
因该 fixture 已脱离原生宿主租约，输出阶段出现 `HOST_SESSION_ID_UNAVAILABLE`；这属于测试
运行身份缺失，不代表 Core Tick 失败，也不能记为 Claude L3 通过。

## T680-T687 新鲜 Claude Canary 证据（2026-09-02）

- T680 复现了宿主回写遗漏：Architect 私有业务 outcome 已存在，但 Claude 未执行
  `record-worker-outcome`，旧路径把它误判为 Worker 失败并重试。现行路径已把该中间态投影为
  `worker_attestation_pending`，禁止伪造句柄、重启 Worker 或消耗失败预算。
- T681 证明质量命令不能只从 `[tool.ruff]`/`[tool.mypy]` 推导；使用 PEP 735
  `[dependency-groups].dev` 的空项目也必须得到 `uv run ruff`/`uv run mypy` 命令，且不采纳
  optional-dependencies 作为替代声明。
- T683 的新制品真实 Setup 发现 `uv sync` 生成的 `*.egg-info` 被错误识别为业务源码；现已将
  生成式元数据排除出 Setup 文件基线，避免工具链副作用触发范围闸门。
- T685 发现同一宿主会话的旧 `CONTINUE` lease 残留到 `WAIT_USER`，导致 Stop Hook 把正常用户
  Gate 误报为“Blocked by hook”；现已在同会话进入可交回控制的 Action 时清理旧 lease。
- T686 使用 Build `5.8.0-rc.5+sha256.33a38bdb4a4e0f08` 验证 Setup、PEP 735 命令推导、
  gap scan 和原生 Claude Agent 启动均能进入 Architect；但 Agent 返回后宿主仍先提交了
  `HOST_WORKER_FAILED`，未完成固定的 `record-worker-outcome` 机器回写，最终进入
  `HOST_WORKER_FAILURE_EXHAUSTED`。私有 outcome 和伪造的 spawn receipt 同时存在，说明
  失败不是业务 Worker 失败，而是 Claude 宿主执行器没有遵守回写顺序；T687 已把该保护前移到
  `--tick` 提交前置检查，下一次同类输入将直接返回 attestation 修复 Action。

## 最终 Claude Canary 复核（2026-09-02）

- 最终候选源代码在新鲜 Canary 中已完成 Setup、PEP 735 命令推导、Gap Scan，并真实启动
  Claude 原生 `Agent`。Worker 返回的 handle 为 `a255d4a1d5fbb4c3f`，模型字段按合同记录为
  `unreported`，隔离证据为 `fresh_context`；宿主随后成功执行固定的
  `record-worker-outcome`，并由 Finalize 生成 `spawned=true` 的完整 Worker Attestation。
  这证明 T682 的机器回写合同本身已经打通。
- 随后的 Coordinator Result 使用了错误的 `plate_keys`，Core 正确拒绝该 Result。宿主没有在
  当前 Action 内修复并重新提交，而是错误地继续调用下一次 `tick`，造成 Action generation、
  `work_files` 和 Worker outcome 路径错配，之后出现 `HOST_EVIDENCE_INVALID` / output missing。
  该失败属于宿主结果修复顺序违反协议，不是 Core 把业务 Worker 判错；运行已主动停止，不能计为
  Claude L3 通过。
- T687 的 CLI 前置保护与 generation 回归已通过自动测试，但本次真实运行没有进入“漏回写后提交
  Worker failure”分支，因此不能把 T687 的真实宿主验收写成通过。T682 的首次回写证据有效，
  T685 的 WAIT_USER 真实复验仍缺失，双宿主 L4 继续阻断。

当前结论：本轮把“漏回写误耗失败预算”从 Prompt-only 约束提升为 CLI 提交边界，同时验证了
Claude 原生 Agent 的真实 handle→attestation→finalize 链路；剩余阻断点是宿主必须在 Core 拒绝
Result 时停留在同一 Action 内修复，禁止继续 tick。A008 与发布门禁继续阻断。

## T688 根因修复验证（2026-09-02）

- 事件回放确认了此前真正的放大器：Architect 计划因非法 `plate_keys` 触发
  `ARCHITECT_PLAN_INVALID` 后，旧 `_tick_process_result()` 进入 `guardrail_retry` 并签发
  新 Action，导致同一份业务问题被表现为新的 Worker 生命周期。该路径不是必要的重试，且与
  D47/D68 的同 Action repair 约束冲突。
- 现行 Core 返回结构化 error，CLI 将其投影为当前 `message_id` 的 `repair_current_action`。
  当共享 outcomes 已完整提交时，宿主视图为 `worker_outcomes_committed`、
  `spawn_permitted=false`，并移除 `spawn`；公开 E2E 已验证事件流只保留原有 ActionIssued/ResultAccepted
  数量，不因拒绝候选再推进一次。
- 该修复解决的是“错误路由导致的重复 Worker/路径错配”。它不等同于真实 Claude L3 已通过：
  真实宿主仍需遵守修复后的 finalize→validate→submit 顺序，T685 的 WAIT_USER 真实证据和
  双宿主 L4 仍然缺失。

## 新 Build Claude Canary 复跑（2026-09-02）

- Build：`5.8.0-rc.5+sha256.495c73a3824697e9`；项目：`/tmp/ae-a008-claude-l3-final-boPH61`；
  全新 Claude Code 会话和全新 `.ae-state`。
- Setup、Gap Scan 和 Architect Action 均由宿主连续驱动，EventStore 保持单一 active
  Action；没有生成第二个 Architect Action，也没有手工改写 Core 事实。
- Architect 原生 Worker 返回后，宿主生成了业务 Coordinator payload，但私有 Worker 文件
  使用了不符合合同的顶层业务字段，未形成要求的 `{worker_id,status,payload,summary}` 私有
  artifact；Core 正确拒绝为 `HOST_WORKER_OUTPUT_MISSING`，没有伪造 attestation 或成功
  Result。宿主随后重复执行有限失败路径，最终稳定报告
  `HARD_LIMIT: spawn_retry_attempt=3 exceeds retry_limit=1` 并停止。
- 该结果证明 T688 的“无效 Coordinator 结果不得生成新 Worker Action”路径未被再次触发，
  但也证明真实 Claude 仍未稳定遵守 Worker 私有 outcome 合同。因此本次不是 Claude L3
  通过证据；A008、WAIT_USER 真实证据和双宿主 L4 继续阻断。

## T690 公开回归补强（2026-09-02）

- 第二次全新 Claude Canary 使用 Build `5.8.0-rc.5+sha256.8b8bbd43d86ffbea`，真实完成
  Setup、Gap Scan、Architect、Developer、Critic，并进入 Component Verifier；原生 Worker
  回写链路继续记录了真实 handle、`unreported` model 和 `fresh_context`。
- Component Verifier Worker 返回的 `coverage_map` 使用了越界 design item，Core 正确拒绝
  `COMPONENT_VERIFICATION_SCOPE_INVALID`。宿主随后先执行 `--validate-result`，再执行
  `--tick`；旧实现因 journal 已从 `prepared` 变为 `rejected`，第二次调用无法识别已有拒绝，
  输出裸 error，宿主停止在当前 Action。
- T690 已将该边界修复为：`--tick` 在收到同一 Result 的持久化 rejection 后仍投影同一
  `repair_current_action`，保留 `message_id`、隐藏 `spawn`、复用已提交 outcomes；公开跨进程
  黄金轨迹已覆盖错误结果→validate→tick→修复→继续到 `TERMINAL`。本次 Claude 会话发生在
  修复前，不能倒填为真实 Claude L3 通过；仍需新制品重新复验。

## T690 新制品真实宿主复验（2026-09-02）

- 使用 Build `5.8.0-rc.5+sha256.37b511aad170c945` 和全新项目启动时，Claude 原生入口已完成
  `LoopInitialized → Gap Scan accepted → Architect Action`，没有 Python Supervisor、重复
  Architect Action 或状态源冲突。
- 原生 Worker 未形成私有 outcome，宿主报告 `HOST_WORKER_ATTESTATION_MISSING` 并将 Action
  保留在 `WAIT_RESOURCE`。宿主声称 `block-secrets-write.sh` 拦截 `.ae-state`，但对同一命令
  做独立 Hook 合同检查返回 `{"decision":"allow"}`；当前证据只能判定为 Claude 宿主/模型对
  阻断原因的误判或原生 Agent 未执行，不能归因给 Core，也不能绕过安全 Hook。
- 该次运行没有进入 T690 的 Component Verifier repair 分支，因此 Claude L3、WAIT_USER 真实
  证据和双宿主 L4 仍未完成；A008 继续保持阻断。

## T691 WAIT_RESOURCE 边界修复（2026-09-02）

- 真实 Claude L4 Setup 复验暴露了第二层问题：Core 已返回
  `PROJECT_SETUP_RETRY_EXHAUSTED / WAIT_RESOURCE`，但宿主仍按新的失败 Result 继续驱动
  `--tick`，使等待态看起来像“循环中断后又自增重试”，失败计数和 Action 快照随之漂移。
- 根因是 Setup 的失败路径在达到阈值后仍无条件递增 `project_setup_failure_streak` 并调用
  `build_action`；这与 `WAIT_RESOURCE` 的 yield 语义不一致。修复后，当前 active Action 已是
  `resource_wait` 时，重复失败只返回同一等待 Action，不写领域事件、不改变计数、不制造新
  Worker/Action；宿主完成项目能力修复后，合法 `project_setup_completed` 仍可恢复到
  `architect`/`gap_scan`。
- 新增公开 CLI 回归覆盖“连续三次失败→重复新 Result→事件流不变→修复后恢复”，定向 289 项、
  全量 `2783 passed, 1 skipped`、覆盖率 `90%`。该修复解决 Core 的重复驱动放大器，但不替代
  真实宿主遵守 `WAIT_RESOURCE` 后停止并等待资源的要求。

## T691 后真实 Claude L4 复验（2026-09-02）

- 使用 T691 Build `5.8.0-rc.5+sha256.b31d05e7b108e929` 和全新项目
  `/tmp/ae-a008-claude-l4-t691-pikeaX`。项目预先只提供 `package.json`、`tsconfig.json`、
  `ae.toml`、空的 `src/`/`tests/`、设计文件和决策账本；没有预写业务源码或测试。
- 首轮被 Claude Bash 审批拒绝，Runner 未执行、`.ae-state` 未初始化；第二轮使用一次性
  `bypassPermissions` 后，Claude 补齐了标准 feature 配置，真实完成
  `LoopInitialized → ProjectProfileResolved → gap_scan → gap_review`。Setup 没有生成业务
  脚手架，说明“能力声明先行 + Setup capability-only”路径可用。
- Claude 在首次 Gap Scan Result 被 Core 以 `clarity=missing` 拒绝后，按同一 Action 修复为
  `clarity=vague`，重新 finalize、validate、tick；EventStore 最终为 10 个事件，active
  Action 为 `gap_review`，`current_gap_index=0`，`total_gaps=2`，Execution Control 为
  `WAIT_USER`。一次误重复消费已删除 Result 只产生 `RESULT_PARSE_ERROR`，没有改写状态。
- 当前不能把这次记为 L4 通过：设计本身缺少组件清单和 MiniMax API 详细契约，Core 正确要求
  用户逐项决策；需要用户先决定当前 Gap 的合法 resolution，之后才能继续 Developer/Verifier
  和真实业务 `typecheck`、`unit test`、`build`。
### T692：旧配置闸门导致首个 Action 前中断（已修复）

真实 Claude L4 的初始项目只包含 ProjectProfile 所需的 `[project]` 元数据时，旧
`_check_config_gate` 将其判为“未包含有效配置”，在 `dev-loop --init` 返回 `CONFIG_REQUIRED`；
无 `ae.toml` 时还会在首个 Action 前自动写入 standard profile。这与 `FeatureManifest` 默认值、
本地 ProjectProfile 探测和主 Agent 单命令驱动设计冲突。

修复后删除 `config-policy`、`AE_CONFIG_POLICY`、`AE_SKIP_CONFIG_CHECK` 和强制配置闸门；
`RuntimeConfig.from_project` 仍按“环境变量 > ae.toml Feature 覆盖 > FeatureManifest 默认值”
读取，`ae.toml [project]` 只由 ProjectProfile Provider 消费。无配置的公开 CLI 现在直接进入
`project_setup_required`，显式 `doctor --init-config/--wizard` 仍可主动生成或修改运行时配置。
回归：93 项配置/CLI 定向测试，公开 CLI 无配置冒烟，`2775 passed/1 skipped`，覆盖率 90%。

真实 Claude 新 Build 复验：`/private/tmp/ae-a008-claude-l4-t692.9gGvAD` 未预置
`ae.toml`，首个 Action 直接进入 `gap_scan`；Claude 修复 Gap Scan 误分类后进入 Architect，
又修复 obligation 映射错误并推进 Developer B1。过程中未出现配置向导、`CONFIG_REQUIRED`、
第二个 Loop 或重复 Worker。由于 Developer 后续宿主任务超过 15 分钟无新状态，按规则正常停止；
该证据证明 T692 和真实同 Action repair 生效，但不能冒充 L4 `TERMINAL`。

### T693/T694：原生返回交接与 Node Setup 范围误判

新 Build `5.8.0-rc.5+sha256.ecd2347f91c32de0` 的真实 Claude 复验项目为
`/tmp/ae-a008-claude-t693.A4xZ9D`，无 `ae.toml` 启动并完成项目工具链 Setup。Claude 按
Setup 合同创建了 `src/main.tsx`、`tests/smoke.test.ts`、Vite/TypeScript 配置和一次性
Vitest 命令；Setup Result 的 finalize/validate/submit 本身成功，但 Core 下一 Tick 将
`src/main.tsx` 判为新增业务实现，并返回 `setup_scope:business_implementation`。

这是 Core 范围判定缺陷，不是 Stop hook 根因：该 capability-only 阶段禁止实现业务，宿主
无法满足这个新缺失项，Claude 正确观察到 Action 不可收敛后尝试停止；Stop hook 依据仍为
`CONTINUE` 的 RunLease，连续返回 `AE_CONTINUATION_REQUIRED`，最终宿主报告
`Stop hook feedback: Blocked by hook`。因此 Stop hook 没有放大或制造状态损坏，它只是暴露了
Setup 自旋未被修复的事实。

T694 已将最小非业务入口定义为跨语言、可审计的有限规则：`source_roots` 下的 `main/index`
入口须有明确 `minimal smoke`/`setup smoke` 标记，或满足严格的 React/Vite bootstrap 形状；
`App` 仅允许无 import、无副作用的单一静态占位，`vite-env.d.ts` 与受限 `test/setup` 工具
初始化文件放行，其他未标记源码仍产生 `PROJECT_SETUP_SCOPE_VIOLATION`。T694c 新制品真实
Claude 已验证空 Node/Vite Setup 通过并进入 `gap_scan`；随后在 `gap_review` 因原始设计
缺少 11 个组件的名称、接口和数据流而合法 `WAIT_USER`，不是 Loop 中断。T695 另收口了
`WAIT_RESOURCE` 后任意失败摘要的只读幂等回放，避免宿主猜测 failure_code 并制造第二个
协议死结。后续复核又发现，Setup 越界文件被错误投影为
`missing_capabilities=setup_scope:business_implementation/business_tests`，让宿主看到
“既禁止又要求”的伪任务；该路径已改为保留原始能力缺失、把越界文件作为反馈事实，并在
compact 宿主视图中保留有界修复反馈。Finalizer 也已对所有 Action 强制绑定当前
`host_execution.work_files`，不再回退到调用者传入的旧路径。此前 T693/T694b 旧 Build
的 Setup 阻断保留为回归历史；原生 Worker ingestion 仍需在设计充分场景下真实验证，不能
记为双宿主 L4 通过。最终 Build `5.8.0-rc.5+sha256.507eda108076ed6e` 的真实 Claude
canary 又验证了三次 Setup 越界后稳定进入 `WAIT_RESOURCE`，宿主得到明确反馈并停止，
没有继续自旋；该 canary 使用 `src/__tests__`（不在该项目声明的 `tests` 根下），被判定
越界属于正确行为，不能与已覆盖的 `src/smoke` 与源码根重叠合法场景混淆。

### T697：Setup 首次 Profile 误判 `src` 内测试文件（2026-09-02）

T696 修复后的新 Build 真实 Claude canary 在空 Node/Vite 项目中使用常见布局
`src/smoke.test.ts` 与 `src/setupTests.ts`。由于首次 ProjectProfile 尚未声明测试根，
Core 原先按 `source_roots=src` 把两者归为 `business_implementation`；宿主据此删除了自己
刚创建的工具链测试。这个反馈证明 T694 的入口放行规则仍只覆盖了入口文件和已声明/嵌套测试根，
没有覆盖“测试根与源码根重叠且测试根尚未被探测”的常见 Vite/Vitest 布局。

T697 增加了安全的最小测试判定：`smoke.test/spec`、`setupTests`、`vitest.setup` 和
`jest.setup` 只有在文件足够小、仅导入允许的测试/框架包且不导入相对路径业务模块时放行；
导入 `./voice-clone` 等业务模块的测试即使文件名含 smoke 也归入 `business_tests` 并拒绝。
`src/__tests__`、显式测试根和源码根重叠场景均有回归覆盖。首轮 canary 因该范围误报在
Setup 中断，未到 Gap Review；修复后同一发布批次重新构建并完成真实宿主复验。

最终 Build `5.8.0-rc.5+sha256.81dd605578c94d62` 的第二次 Claude canary 使用全新项目
`/private/tmp/ae-t697-project.OduNFj`，实际创建 `src/__tests__/smoke.test.ts`、
`src/setupTests.ts` 和 Vite React 工具链；项目 build、test、lint 均通过，Setup Result
成功 finalize/validate/submit，下一步进入 `gap_scan`，再进入 `gap_review/WAIT_USER`。
Claude 只展示当前 `GAP-1` 的问题、影响和合法选项，没有展示或询问 GAP-2/GAP-3，证明
T696 的单项宿主契约在该真实会话中生效。设计文档仍然不足以自动作出产品决策，故 canary
在合法 WAIT_USER 停止；Codex/Claude 完整 L4 仍需继续执行。

### T696：真实宿主复验暴露 Gap Review 历史泄漏（2026-09-02）

最终 Build `5.8.0-rc.5+sha256.86aaf202e8db05d1` 的真实 Claude canary 已完成
`Project Setup → gap_scan → gap_review/WAIT_USER`。Core 返回的当前 Action 只包含
`current_gap=voice-setting-schema`，并且当前 Coordinator Prompt 明确要求单项向导；但
Claude 仍从会话历史中的完整 `gap_scan` 结果取出 3 个 gap，一次性展示并要求用户批量回答。
这不是 Core 游标错乱，也不是一次合法的用户 Gate，而是宿主展示层没有把历史消息降级为非权威
上下文，可能导致批量提交和用户误决策。

修复包括：Gap Review Action 增加必选 `gap_review_contract`（`current_gap_only`、单决策、
`current_gap.id` 绑定及禁止历史/未来/批量上下文）；compact/status 视图显式保留该契约；
Command、Skill 和 Action instruction 明确禁止复述、枚举、摘要或批量询问历史缺口；schema
缺少该字段时 fail-closed。回归新增 schema、Core Action、公开 status/compact 断言；随后全量
`2789 passed, 1 skipped`，覆盖率 `90%`，Ruff、mypy、compile、diff check 均通过。该修复仍需
在新 Build 上真实 Claude/Codex L4 复验，不能把 prompt/contract 回归测试当作宿主终态证据。
### T698/T699：原生句柄合同与未来改进来源边界（2026-09-02）

T698 在宿主层统一了原生 Worker 句柄、私有 outcome 回写和同 Action
`repair_coordinator_then_finalize` 恢复语义；缺少原生句柄时 CLI 结构化失败并 fail-closed，禁止
启动回显 Agent、伪造 completed Worker、复用旧 Action 的 work files 或重复 spawn。新 Build
`5.8.0-rc.5+sha256.e53570bd5b48de66` 的 Claude staging 安装成功，真实 Claude 观察到
`task_started.task_id` 原生句柄和当前 Action 隔离文件；该 canary 的旧 Gap Scan 结果曾被 Core
拒绝一次后在同一 Action 修复并进入 `gap_review`，未发生新的 Worker Action。

同一 canary 又暴露 T699：设计文档明确把 `§13.3 未来改进方向` 标为路线图，但模型把 CORS
等未来风险产出为 GAP-2，令 Core 合法进入 `gap_review/WAIT_USER`；GAP-1 还把未来章节放进
evidence。问题不在单项游标，而在 Core 缺少设计来源层级校验。T699 已增加 DesignDoc 对
明确未来/咨询章节的确定性识别，Gap Scan 同时校验 `design_section_ref` 与 `evidence`，命中
未来章节返回 `GAP_ANALYSIS_FUTURE_SCOPE_MISCLASSIFIED` 并留在当前 Action 修复；当前章节
之间的真实契约矛盾仍可报告。相关 267 项定向回归、全量 `2798 passed/1 skipped`、覆盖率
`90%`、Ruff/mypy/compile/diff check 已通过；新 Build 真实宿主复验仍待执行，A008 不关闭。

### T700：Setup smoke 与测试门禁合同收口（2026-09-02）

T699 canary 的下一次复验发现两个新的 Setup 合同断点：宿主创建的最小 smoke test 通过
`../src/index` 验证 setup-safe 入口时，被“禁止一切相对导入”的规则误报为业务测试；标准
React 占位 `App` 带静态 `id/data-testid` 属性时，也没有命中过窄的单一 `<div>` 规则。另一个
可见风险是 Node 项目 `scripts.test = vitest` 默认进入 watch 模式，Setup Gate 可能等待到
超时，宿主随后把门禁失败误判成 Loop 中断。

T700 保持 ProjectProfile 命令唯一来源，不自动拼接或改写用户命令：Core 只解析相对 import
到项目根内的候选文件，并要求目标再次通过 setup-safe 占位判定，同时允许无副作用静态 div
属性但拒绝事件处理器、脚本协议和表达式；ProfileCommandGate 在启动项目测试前拒绝已知
Vitest/Cypress/Playwright/Jest watch/UI 模式，并返回 `PROJECT_TEST_COMMAND_INTERACTIVE`
及明确修复方式。新增定向回归 37 项，相关模块 273 项，全量 `2801 passed/1 skipped`，覆盖率
90%，Ruff/mypy 通过；新 Build 真实双宿主复验仍待执行，A008 继续阻断。

### T700 真实宿主复验（2026-09-02）

- 新 Build `5.8.0-rc.5+sha256.a4521d25e5049f68` 的 Claude 真实宿主从空 Node/Vite 项目开始，完成 `project_setup`、一次范围越界拒绝后的同 Action 修复、Setup smoke/test/lint/build，再进入 `gap_scan → gap_review`；最终稳定返回单项 `WAIT_USER`，没有第二个循环，也没有进程异常中断。该轨迹证明 T700 的 Setup-safe 本地入口、非交互 `vitest run` 和范围反馈可以被真实宿主消费。
- 同一临时工程的 Codex 真实命令实际加载了本机旧缓存 `/Users/minjianq/.codex/plugins/cache/auto-engineering/auto-engineering/5.8.0-rc.5/bin/ae-run`，Core build id 为 `5.8.0-rc.5+source.sha256.1afd4e185ac29b68`，不是本次 T700 Build；旧 runner 在 Setup 失败修复后仍返回 `PROJECT_SETUP_RETRY_EXHAUSTED / WAIT_RESOURCE`。这不是 T700 新实现的失败证据，反而确认必须把“制品身份→宿主加载路径”作为验收前置条件。
- 本轮未修改用户项目和用户级 Codex 配置；Codex 新 Build 真实 L4 仍待在获得明确安装/切换授权后复验。A008 与发布门禁继续阻断，不能把 archive smoke 或旧缓存运行结果记为新 Build L4。
- 隔离 `CODEX_HOME=/tmp/ae-t700-codex-home-20260902` 已从 T700 Marketplace 安装出插件缓存，`codex plugin list` 显示路径为该 Build；但隔离环境没有登录凭据，真实 Codex CLI 在模型调用阶段返回 `401 Unauthorized / Missing bearer`，未进入 `dev-loop --init`。因此 T700 的 Codex 真实轨迹仍缺少“认证可用”这一外部前置，不能用该次失败评价 Core 或 Loop。

### T701：Codex 安装身份校验补强（2026-09-02）

T701 针对上述旧缓存现象补上安装验收缺口：`verify_codex_install()` 不再只用字符串包含判断
Marketplace/plugin 路径，而是解析 Codex 原生 `plugin list --json`，定位目标插件的
`source.path`，读取并校验其 `build-info.json`，要求 `build_id` 与候选 Release 完全一致且
`content_sha256` 一致。缺失、损坏、插件未列出或内容漂移均在启动独立运行时前 fail-closed。

新增回归先以错 Build 证明旧实现会放行，再以新实现拒绝；安装/产品相关回归 `48 passed`，
全量 `2802 passed, 1 skipped`，覆盖率 `90%`，主包和安装脚本 mypy、compile、规则同步、
diff check 通过。最终候选 Build 为 `5.8.0-rc.5+sha256.15dced478c93674b`，Codex/Claude
archive smoke 均通过。T701 解决了验收阶段的旧缓存误判，但没有替代真实宿主认证和用户级
插件切换；Codex 新 Build L4 仍待授权执行，A008 继续阻断。

### T702：真实 Codex Architect 暴露 canonical section 引用缺口（2026-09-02）

隔离 `CODEX_HOME` 复用已认证 Codex 后，最新 T701 Build 首次完成了 Setup、Gap Scan 和
Architect Worker。Architect Worker 返回的 `batch_plan.design_sections` 使用了板块标题
`B1 Greeting capability`，而不是 Core 工程模型要求的 canonical `§C1`；Core 正确返回
`ARCHITECT_PLAN_INVALID / ENGINEERING_SECTION_UNKNOWN`。

该次运行证明了方案 A 的恢复边界：Worker outcome、native handle、generation 和 fencing
token 已先固化，宿主没有重新 spawn；只修复同一 Action 的 Coordinator payload，重新
`finalize → validate → tick` 后进入 Developer。根因是 Architect Prompt 只写“章节标题”，
没有明确绑定 `host_design_sections[].section_ref`。T702 已将 Prompt 改为 exact canonical
引用要求，并增加回归测试；新 Build 复验需确认未来真实 Worker 不再产生该别名。

### T703/T704：flat design 与状态协调 Gate 的真实宿主复验（2026-09-02）

T702 修复后的新 Build 在 Codex L4 已按 `§C1` canonical section 运行到
`TERMINAL`。同一 Build 的 Claude L4 使用只有 H2 板块、没有 H3 Component 的最小设计文档，
Core 暴露了 `valid_plate_keys=[]` 与 `BatchState.plate_keys` 非空不变量之间的矛盾。T703
增加确定性的 plate-backed 执行单元、允许 `§` section 前缀，并补充 Action/BatchState/DesignDoc
回归；不改变设计文档，也不放宽非空路由约束。

该 Claude 轨迹随后因 Setup 修复期间宿主错误执行 `--init` 触发
`STATE_RECONCILIATION_REQUIRED`。Core 正确阻止旧状态被静默重置，但状态协调 Gate 的 repair
投影只带通用“修复 Result”提示，未显式给出嵌套 `gate_resolution` 的机器合同，宿主误提交了
顶层 `decision`。`finalize` 能生成 Result，但 `tick` 按 Gate 专用校验拒绝并稳定停在
`GATE_RESOLUTION_REQUIRED`；没有假通过、没有第二个 Python loop、没有重复 Worker。

T704 已补齐：状态协调 Gate 初始 Action 与同 Action repair Action 均下发唯一
`gate_resolution.{gate_id,resolution}` 的 `expected_format/result_contract`，并明确禁止顶层
`decision/gate_id`；Host Finalizer 对 `action=gate` 跳过普通 stage Result 必填字段，只交给 Core
执行 Gate 语义校验。

### T705：同一 Build 双宿主 L4 完成（2026-09-02）

T705 修复了 T704 真实 Claude 轨迹继续暴露的两个实现缺口：flat H2-only 设计下
`ProgressTree` 只有 plate 没有可稳定承载进度的隐式 component，导致 `BatchState` 与进度投影
不一致；Gap Scan 对带 `§` 的 canonical section alias 也不兼容。实现增加 plate-backed implicit
component 的稳定 identity、跨 sync 保持 identity，并允许 canonical alias 映射到真实 section；同时
修复安装脚本 Ruff B009。相关 ProgressTree/BatchState/Gap/Action 回归通过。

候选 Build `5.8.0-rc.5+sha256.fc103edee2678253` 完成最终门禁：全量 `2810 passed/1 skipped`、
覆盖率 `90%`，Ruff、mypy、compileall、规则同步、双宿主 archive smoke 全部通过。隔离真实 Codex
与 Claude 均从 Setup 连续运行到 `TERMINAL/GOAL_ACHIEVED`，并验证了同一宿主链中的 fail-closed
恢复：Claude 首次漏传 native handle、Critic Assurance Bundle 结构/计数错误时，Core 分别拒绝并
要求同 Action repair；宿主补齐事实后未重启 Worker、未创建第二套循环。该结果满足 A008，发布阻断
解除。残余仅为测试基线 SQLite `ResourceWarning` 和临时验收项目自身的非阻塞 P2 观察，不影响本仓库
核心实现或产品终态。
