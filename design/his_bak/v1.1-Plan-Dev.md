# Auto-Engineering v1.1 — 开发计划（整合版）

> 来源：@design/INDEX.md | 创建：2026-06-24 | 更新：2026-06-25
> 整合：v1.1-TODO-LIST + v1.1-UNIFIED-DEV-PLAN → 本文档
> 关联：@design/BEACON.md（项目明灯）

---

## 一、问题清单（P0/P1/P2）

> 来源：v1.1-Audit-Report.md ultrathin 审计
> P1 已全部完成（2026-06-25 commit），状态见各节标记。

### 严重度说明

| 级别 | 含义 | 处理优先级 |
|------|------|-----------|
| **P0** | 阻断 dev-loop 真可用 | 立即修（本 session） |
| **P1** | 重要缺陷，影响产品体验或安全 | 优先修（下个 session） |
| **P2** | 架构演进 / 协作改进 | v2.0 / 长期 |

### P0 阻断（2 项，新增）

无新 P0(本会话前已识别)。**本轮新增：**

| # | 标题 | 依据 |
|---|------|------|
| **P0.1** | Agent Tools 完全未连接（dev-loop 无法真运行） | `base.py:87` self.tools 永远为空；`architect.py:46` `tools=[]` |
| **P0.2** | SearchCodeTool 路径遍历漏洞 | `file_tools.py:139` 未调用 `_is_path_safe` |

---

### P1 重要（14 项，8项已完成，新增6项）

| # | 标题 | 状态 | Commit/来源 |
|---|------|------|------------|
| **P1.1** | cli.py TokenTracker 是 dead code | ✅ 完成 | `773c384` |
| **P1.2** | dev-loop CLI 默认用 ScriptedMockRuntime | ✅ 完成 | `08d56a0` |
| **P1.3** | BaseAgent LLM 错误无分类 | ✅ 完成 | `66e03a3` |
| **P1.4** | Tool 错误是字符串而非 AEError | ✅ 完成 | `894722e` |
| **P1.5** | Tool 执行无沙箱/权限控制 | ✅ 完成 | `e54e5ca` |
| **P1.6** | 项目级 ruff 41 个手工 lint 债 | ✅ 完成 | `3b76826` |
| **P1.7** | BaseAgent.execute 工具参数无校验 | ✅ 完成 | `b6f9a4a` |
| **P1.8** | cli.py dev_loop 错误归类 TokenTracker.add 未真接 | ✅ 完成（=P1.1） | `773c384` |
| **P1.9** | Agent 初始化时未注入 project_root（白名单无效） | 新增 | §二 Phase 0.1 修复 |
| **P1.10** | cli 进度输出 tokens 永远为 0 | 新增 | §二 Phase 1 |
| **P1.11** | AEError 被 bash_tools 转换为 ToolResult（错误分类丢失） | 新增 | §二 Phase 0.2 |
| **P1.12** | init A1 增量模式设计与实现不符（跳过已存在 vs MERGE） | 新增 | §二 Phase 2.1 |
| **P1.13** | RunTestsTool._detect_runner 默认 pytest（非 Python 项目误判） | 新增 | §二 Phase 0.2 |
| **P1.14** | cli.py dry_run 参数未实现 | 新增 | §二 Phase 1 |

---

### P2 建议（14 项，8项来源已存，新增6项）

| # | 标题 | 维度 | 优化建议 |
|---|------|------|---------|
| **P2.1** | Channel 类型系统缺失 | 架构 | v2.0 引入 Channel 抽象（LastValue/Accumulating/Barrier） |
| **P2.2** | PUSH 动态路由未实现 | 架构 | v2.0 实现 Send PUSH（Architect 拆分 → runtime 分派多个 sub-Agent） |
| **P2.3** | Agent 缺 role/goal/backstory 字段 | 架构 | Agent dataclass 加 role/goal/backstory/tools_config 字段 |
| **P2.4** | 缺 streaming 进度输出 | 工程化 | BaseAgent 加 streaming 模式（async generator） |
| **P2.5** | system_prompt 缺版本管理 | 工程化 | 每 system_prompt 加 `PROMPT_VERSION = "v1.0"` |
| **P2.6** | 缺 CONTRIBUTING.md | 协作 | 加 CONTRIBUTING.md（架构图 + 代码组织 + TDD 流程） |
| **P2.7** | 缺 API 文档（sphinx） | 协作 | 加 docs/api/ + sphinx 配置 |
| **P2.8** | 中英 prompt/UI 混用 | 协作 | 统一英文（国际开源趋势） |
| **P2.9** | CheckpointStore 无清理机制 | 工程化 | AE v1.1 checkpoint TTL 过期删除 |
| **P2.10** | ListDirTool 无路径安全检查 | 架构 | 调用 `_is_path_safe` 与 WriteFileTool/EditFileTool 一致 |
| **P2.11** | Agent.system_prompt 无法动态更新 | 架构 | 注入 project_root 等动态上下文到 system_prompt |
| **P2.12** | RunTestsTool._detect_runner 依赖 cwd | 代码 | cwd 为 None 时返回错误而非 fallback pytest |
| **P2.13** | 3 Agent max_tool_calls 全用默认值 | 工程化 | architect 只读场景降低配额 |
| **P2.14** | AEError.error_code 未在 ToolResult 中传递 | 代码 | ToolResult.error_code 设置路径 |

---

## 二、未完成 Phase（按依赖排序）

### Phase 0：dev-loop 真接（让 BaseAgent 真调 Claude API）

> **本轮审计新增 P0.1**：Agent Tools 完全未连接，是 Phase 0 的前置阻断。

| # | 任务 | 来源 | 依赖 | 行数 | 优先级 |
|---|------|------|------|------|:-----:|
| **0.0** | **P0.1 修复：Agent Tools 连接**（BaseAgent.execute 用 task.tools 解析为 BaseTool 实例 + registry 传入 project_root） | D2.02 | — | ~100 | **P0** |
| 0.1 | **BaseAgent 真接**（LLM 调用循环 + 工具循环 + max_tool_calls + output_schema + parser 双层防御） | D2.02 + dev-loop-TODO C3c | llm + parser ✅ + 0.0 | ~200 | **P0** |
| 0.2 | **Tools 真接**（registry + file/bash/git/test 4 类 + 路径白名单 + timeout） | D2.01 续 | 0.1 + **P0.2 SearchCodeTool 安全修复** | ~100 | P0 |
| 0.3 | **3 Agent 真接**（architect/developer/critic + system_prompt + tools 绑定） | D2.03 | 0.2 | ~200 | P0 |

### Phase 1：dev-loop CLI 完整化（Plan D3）

| # | 任务 | 来源 | 依赖 | 行数 | 优先级 |
|---|------|------|------|------|:-----:|
| 1.1 | `ae checkpoint list/resume/show` 命令 + CheckpointStore API | D3.01 | engine/checkpoint ✅ | ~150 | P0 |
| 1.2 | CancellationToken 贯穿 loop.py + BaseAgent | D3.01 | cli + loop | ~100 | P0 |
| 1.3 | TokenTracker 真接（累加 LLMUsage + 超阈值抛错） | D3.01 | cli + runtime + 0.1 | ~100 | P0 |
| 1.4 | 真进度输出（stage_done 即时，删 pre-echo 假象） | D3.01 | cli + 0.1 | ~80 | P0 |
| 1.5 | 端到端真实 LLM 验证 | D3.02 | 0.3 + 1.1-1.4 | manual | P0 |

### Phase 2：init 子系统 §1.3 接口规范 100% 落地

| # | 任务 | 来源 | 依赖 | 行数 | 优先级 |
|---|------|------|------|------|:-----:|
| 2.1 | **A1 增量模式完整实现**（incremental / _created_files / _mode + MERGE 逐文件合并 + 自动检测 + 跳过 .git/ + cli --incremental） | init-TODO-A1 | 无 | ~150 | **P0**（唯一阻塞） |
| 2.2 | A2 _phase_tasks 传递 current_phase 给 TaskRunner | init-TODO-A2 | 无 | ~3 | P1 |
| 2.3 | A3 _run_builtin_hooks git commit 失败改为非阻塞 | init-TODO-A3 | 无 | ~3 | P1 |
| 2.4 | A4 symlink 文件处理（shutil.copy2 保留链接） | init-TODO-A4 | 无 | ~8 | P1 |
| 2.5 | A5 ProjectEnvironment._warn_undetectable(root) 方法 | init-TODO-A5 | 无 | ~5 | P2 |
| 2.6 | A6 FRAMEWORK_SIGNATURES spec-doc 加 design/v*.md 通配 | init-TODO-A6 | 无 | ~1 | P2 |
| 2.7 | A7 `_from_detection` package_manager 默认值 "npm" | init-TODO-A7 | 无 | ~1 | P3 |

### Phase 3：init 验证 + 测试覆盖

| # | 任务 | 来源 | 依赖 | 行数 | 优先级 |
|---|------|------|------|------|:-----:|
| 3.1 | B1 E2E 实跑 `ae init my-project --type app-service --defaults` | init-TODO-B1 | 2.x | 1h | P0 |
| 3.2 | B2 8 种项目类型 E2E 验证 | init-TODO-B2 | 3.1 | 2h | P0 |
| 3.3 | B3 存量项目增量模式 E2E | init-TODO-B3 | 2.1 | 1h | P1 |
| 3.4 | **R26 设计文档目录规范化（ae init 内置）** `design/INDEX.md + BEACON.md + his_bak/` | 用户需求（2026-06-25） | 无 | ~50 | P1 ✅ |
| 3.5 | C1 补 detector/hooks/config/renderer/errors 单元测试（覆盖率 ≥80%） | init-TODO-C1 | 2.x | ~300 | P0 |
| 3.6 | C2 scaffold.py 端到端 E2E 测试（5 阶段流水线 + 失败清理 + 增量模式） | init-TODO-C2 | 2.1 | ~200 | P1 |

### Phase 4：文档同步 + 项目级 ruff 债清理

| # | 任务 | 来源 | 依赖 | 行数 | 优先级 |
|---|------|------|------|------|:-----:|
| 4.1 | 同步 `v1.0-Design-INIT.md` §1.8 待办状态 + R26 状态 | init-TODO-D1 | Phase 3 | 1h | P1 |
| 4.2 | 更新 `BEACON.md` 当前状态 | init-TODO-D2 | 全部 | 1h | P1 |
| 4.3 | 项目级 ruff 债清理（RUF012 ×11 / SIM117 ×9 / B904 ×7 / 其他 ×5 = 32 errors） | his_bak/v1.1-LOOP-ROADMAP P2 | 无 | ~60 | P1 |
| 4.4 | 模板 `.gitignore.tmpl` 增 `coverage/` 排除 | multi-agent §6 | 4.3 | ~1 | P2 |
| 4.5 | 模板 `_scratch/<agent-id>/coverage/` 约定文档 | multi-agent §6 | 4.4 | doc | P2 |
| 4.6 | 测试改进：`tests/test_init_design_docs.py`（5 E2E）+ `--skip-tasks` 标志应用 + block detector 缓存清理 | 2026-06-25 | 无 | ~100 | P2 |

### Phase 5：v2.0 多 Agent 并行（后置，基于 v1.1 完成）

| # | 任务 | 来源 | 依赖 | 行数 | 优先级 |
|---|------|------|------|------|:-----:|
| 5.1 | Architect prompt 增加文件集预检输出要求 | multi-agent §6/§8.2 | Phase 1 | ~50 | P2 |
| 5.2 | 契约确认机制（two-green gate，Agent 启动第一步） | multi-agent §四 | 5.1 | doc+prompt | P2 |
| 5.3 | templates/app-service/.worktreeinclude.tmpl 恢复 | multi-agent §3.5 | 4.4 | ~10 | P2 |
| 5.4 | Channel 系统 + Orchestrator（v2.0-LOOP-ANALYSIS Phase 5） | v2.0-LOOP-ANALYSIS | Phase 1 | 大量 | **v2.0 后置** |

---

## 三、依赖图（全项目）

```
[Plan A bugfixes ✅]
[Plan B.01 config ✅]
[Plan D1 runtime-guardrail ✅]
[Plan D2.01 llm+parser+tools test ✅]
        ↓
[0.1 BaseAgent] → [0.2 Tools] → [0.3 3 Agent]
        ↓                        ↓
   [1.1 checkpoint]    [1.3 TokenTracker]
        ↓
   [1.2 Cancellation]（loop.py 改动，与 1.1 可并行）
        ↓
   [1.4 真进度输出]
        ↓
[1.5 真实 LLM E2E]
        ↓
[Phase 2 init 修复 2.1-2.7] ── [Phase 3 init 验证 3.1-3.5]
        ↓
[Phase 4 文档 + ruff 债 4.1-4.5]
        ↓
[Phase 5 v2.0 多 Agent 5.1-5.4 后置]
```

---

## 四、执行顺序与并行性

### 强串行（不能跨阶段）

- **0.1 → 0.2 → 0.3**（BaseAgent → Tools → 3 Agent，Tool 接口被 Agent 用）
- **0.3 → 1.1-1.4**（dev-loop Phase 3 完成才能接 CLI）
- **1.x → 1.5**（CLI 完整才能端到端验证）
- **Phase 2 → Phase 3**（init 修复后才能验证）
- **Phase 3 → Phase 4**（验证通过后才能更新文档）

### 可并行（独立轨道）

| 并行组合 | 说明 |
|---------|------|
| **Phase 0 与 Phase 2** | dev-loop 真接 与 init 修复完全独立（无共享代码） |
| **Phase 4.3（ruff 债）与任何阶段** | 仅 lint 修复，独立 |
| **1.1（checkpoint）与 1.2（Cancellation）** | 可交错 commit |
| **Phase 4.4-4.5（模板）与 Phase 4.3（ruff）** | 模板独立 |

### 推荐启动序列

```
[Day 1-2]  Phase 0.1 BaseAgent（串行）
[Day 2-3]  Phase 0.2 Tools（可与 Phase 2.1 并行）
[Day 3]    Phase 0.3 3 Agent（串行，依赖 0.2）
[Day 3-4]  Phase 1.1-1.4 CLI 完整（并行 commit）
[Day 4]    Phase 1.5 真实 LLM E2E
            ── 并行 ──
[Day 1-3]  Phase 2.1 A1 增量模式（独立轨道）
[Day 3-4]  Phase 2.2-2.7 小修 + Phase 3 验证
            ── 并行 ──
[Day 1-2]  Phase 4.3 项目级 ruff 债清理（~2h，可随时启动）
[Day 4-5]  Phase 4.1-4.5 文档 + 模板
[Day 5+]   Phase 5 v2.0 多 Agent（后置，基于 v1.1 验证后启动）
```

---

## 五、验收标准

### Phase 0（dev-loop 真可用）

- [ ] `ae dev-loop "实现简单函数"` 真调 Claude API（无 mock 数据）
- [ ] BaseAgent.execute() 完整 async 工具循环 + max_tool_calls 上限
- [ ] 3 Agent 各自 system_prompt 真接，output_schema 验证
- [ ] 10 tools 真实 execute（read/write/edit/bash/test/git ×4）
- [ ] LLM 输出双层防御验证
- [ ] ~50 新测试全过（累计 ~215）
- [ ] ruff check Phase 0 文件 0 errors

### Phase 1（CLI 完整）

- [ ] `ae checkpoint list/resume/show` 全部工作
- [ ] Ctrl-C 真中断 + checkpoint.status="drained" 保存 + resume 可继续
- [ ] TokenTracker 累加 LLMUsage + 超 `--max-tokens` 抛 BUDGET_EXCEEDED
- [ ] 进度输出 stage_done 即时（无 pre-echo 假象）

### Phase 2+3（init §1.3 100%）

- [ ] A1-A7 全部修复
- [ ] `ae init --incremental` E2E 验证（增量不覆盖已有）
- [ ] 8 种项目类型 E2E 全部通过
- [ ] init 覆盖率 ≥80%（当前 ~40%）

### Phase 4（文档 + ruff）

- [ ] `make ci` 项目级绿（ruff 0 errors）
- [ ] `v1.0-INIT.md` §1.8 待办状态表已更新
- [ ] `BEACON.md` 当前状态已更新 4 次（Phase 0/1/2+3/4）
- [ ] `coverage/` 加入 `.gitignore.tmpl`

### Phase 5（v2.0 后置）

- [ ] Architect prompt 含文件集预检输出要求
- [ ] 契约确认机制就位（两层绿才推进）
- [ ] `.worktreeinclude.tmpl` 模板恢复（Layer 1 使用）

---

## 六、工作量估算

| Phase | 行数 | 测试 | 工时 | 风险 |
|-------|------|------|------|------|
| Phase 0 dev-loop 真接 | ~1000 | ~50 | ~18h | **高** |
| Phase 1 CLI 完整 | ~430 | ~30 | ~8h | 低 |
| Phase 2 init 修复 | ~170 | ~5 | ~3h | 中 |
| Phase 3 init 验证 | ~500 | ~30 | ~6h | 中 |
| Phase 4 文档 + ruff | ~60 | 0 | ~2h | 低 |
| Phase 5 v2.0 后置 | ~150 | ~20 | ~5h | **高** |
| **合计** | **~2310** | **~135** | **~42h** | |

---

## 七、风险评估

### 高风险

1. **LLM 真实调用的成本控制**：每次 dev-loop 消耗数千 token，必须 budget 限制
2. **Agent 输出解析稳定性**：LLM 输出格式变化，parser 双层防御仍可能失败
3. **Tools 真实执行的副作用**：WriteFile/Bash 可能破坏真实文件系统（必须路径白名单）
4. **Phase 5 v2.0 Channel 改造破坏 v1.0**：Channel 是基础抽象，必须向后兼容

### 中风险

1. **A1 增量模式设计**：scaffold.py 增量逻辑复杂，可能引入新偏差
2. **ruff B904 修复影响异常处理**：`raise X from e` 改变异常链，需逐个判断
3. **CancellationToken + LLM API 中断**：LLM 阻塞时 Ctrl-C 不能立即停止
4. **真实 LLM 端到端测试**（D3.02）：需 ANTHROPIC_API_KEY，成本不可控

### 低风险

1. CLI 命令扩展（标准模式）
2. Tools registry 字符串解析（简单）
3. 文档同步（模板化操作）

---

## 八、明确边界（不在范围）

- ❌ 多 LLM Provider（Anthropic-only v1.0）
- ❌ LangGraph / AutoGen / CrewAI 等其他框架适配
- ❌ Streamlit / Web UI（纯 CLI v1.0）
- ❌ LangGraph Runnable 化
- ❌ 跨会话 checkpoint 远程存储
- ❌ 实时 streaming 进度（v2.0+）
- ❌ 商业化 / 部署 / CI-CD to GitHub Actions
- ❌ Worktree 多 Agent 并行运行（已分析不启用）

---

## 九、原子 commit 建议（共 ~14 commit）

| # | Commit message | 对应 Phase |
|---|---------------|-----------|
| 1 | `feat(agents): BaseAgent 真接 LLM 调用循环 + 工具循环` | Phase 0 |
| 2 | `feat(tools): ToolRegistry + 4 类工具真接(read/write/edit/bash/test/git)` | Phase 0 |
| 3 | `feat(agents): 3 Agent 真接 + system_prompt + tool 绑定` | Phase 0 |
| 4 | `feat(cli): ae checkpoint list/resume/show + CheckpointStore API` | Phase 1 |
| 5 | `fix(loop): CancellationToken 贯穿 LoopEngine + BaseAgent` | Phase 1 |
| 6 | `feat(cli): TokenTracker 真接 LLMUsage + 超阈值抛 BUDGET_EXCEEDED` | Phase 1 |
| 7 | `feat(cli): 真进度输出(stage_done 即时)` | Phase 1 |
| 8 | `feat(init): A1 增量模式完整实现(scaffold.py + cli --incremental)` | Phase 2 |
| 9 | `fix(init): A2-A7 小修复(§1.3 接口规范 100% 落地)` | Phase 2 |
| 10 | `test(init): E2E 验证 + 8 种项目类型覆盖` | Phase 3 |
| 11 | `test(init): 单元测试覆盖率 ≥80%(detector/hooks/config/renderer/errors)` | Phase 3 |
| 12 | `chore(lint): 项目级 ruff 债清理(32 errors)` | Phase 4 |
| 13 | `docs: 同步 v1.0-INIT.md §1.8 + BEACON.md 当前状态 + .gitignore.tmpl coverage/` | Phase 4 |
| 14 | `feat(architect): 文件集预检 + 契约确认机制 + .worktreeinclude.tmpl 恢复` | Phase 5 |

**总计 14 commit，主题清晰，可独立 revert。**

---

## 十、历史来源

| 原文件 | 状态 | 现位置 |
|--------|------|--------|
| `v1.1-TODO-LIST.md` | 合并入本文档 §一 | `his_bak/v1.1-TODO-LIST.md` |
| `v1.1-UNIFIED-DEV-PLAN.md` | 合并入本文档 §二-九 | `his_bak/v1.1-UNIFIED-DEV-PLAN.md` |
| `v1.1-Audit-Report.md` | 保留，历史归档 | `design/v1.1-Audit-Report.md` |
| `BEACON.md` | 保留，项目明灯 | `design/BEACON.md` |

---

## 十一、引用文件

- `@design/BEACON.md` — 项目明灯
- `@design/v1.0-INIT.md` — init 设计
- `@design/v1.0-LOOP.md` — loop 设计 v3.0
- `@design/v1.0-SHARED.md` — 共享架构
- `@design/v1.1-Audit-Report.md` — 架构审计报告
- `@design/v2.0-LOOP-ANALYSIS.md` — v2.0 多 Agent 设计
- `@design/his_bak/v1.1-TODO-LIST.md` — 问题清单原始版
- `@design/his_bak/v1.1-UNIFIED-DEV-PLAN.md` — 统一开发计划原始版

---

*开发计划结束。Phase 0 + Phase 2 可立即并行启动（产品底线 + init 修复）。*
