# Phase 78 Architecture Baseline and Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use `test-driven-development` and execute each checkbox in order; do not write production code before the corresponding RED test.

**Goal:** 修复 rc.5 真实运行暴露的跨 Stage 架构事实、Gate 转移、计划覆盖和确定性返修缺陷。

**Architecture:** 将 Architect 临时输出物化为事件化 ArchitectureBaseline；Gate verdict、
Critic Finding 和 Host Receipt 都作为显式协议输入参与纯 StageHandler 决策。保留 v1.1
兼容字段，并以有界投影避免重新引入长上下文。

**Tech Stack:** Python 3.11+、dataclass/Pydantic、SQLite EventStore、pytest、Ruff、mypy。

---

### Task 1: Gate fail-closed 转移（T389）

**Files:** `tests/test_stage_handlers.py`、`tests/test_tick_orchestrator.py`、
`auto_engineering/loop/stages/developer.py`、`auto_engineering/loop/tick_orchestrator.py`

- [ ] RED：构造 Developer Result 的 required Gate `hard_fail`，断言下一 Action 仍为
  developer、cursor 不 advance、无 `STAGE_ADVANCED developer→critic`。
- [ ] GREEN：把 blocking Gate 摘要放入 TransitionContext；DeveloperHandler 优先返回
  stay-in-stage 决策和结构化 feedback。
- [ ] REFACTOR：统一 `blocking_gate_results()`，兼容 dict/object verdict。
- [ ] VERIFY：`uv run pytest tests/test_stage_handlers.py tests/test_tick_orchestrator.py -q --no-cov --timeout=60`。

### Task 2: ProjectProfile 包管理器命令（T390）

**Files:** `tests/test_project_profile.py`、`auto_engineering/project_profile/providers.py`、
`auto_engineering/project_profile/resolver.py`

- [ ] RED：pnpm+tsconfig+typescript dependency 且无 typecheck script 时期望
  `("pnpm","exec","tsc","--noEmit")`；无 typescript dependency 时不产生 type_check；
  Legacy 裸 `tsc` 不得覆盖本地冲突事实。
- [ ] GREEN：从 dependencies/devDependencies 读取本地工具证据并生成 pm-native 命令；
  resolver 对低优先级未验证命令 fail-closed。
- [ ] VERIFY：`uv run pytest tests/test_project_profile.py -q --no-cov --timeout=60`。

### Task 3: ArchitectureBaseline 投影（T391）

**Files:** `auto_engineering/loop/architecture_baseline.py`、`auto_engineering/engine/state.py`、
`auto_engineering/loop/events.py`、`auto_engineering/loop/projector.py`、
`auto_engineering/loop/tick_orchestrator.py`、`tests/test_architecture_baseline.py`

- [ ] RED：Architect 接受后 clear/resume/replay 仍得到同 baseline_id、plan、contracts、
  obligations；修改 payload 产生不同摘要。
- [ ] GREEN：实现 canonical model、摘要和 `ARCHITECTURE_BASELINE_ACCEPTED` 事件投影；
  ArchitectHandler 通过验证后物化，临时字段仍按原规则清理。
- [ ] VERIFY：运行 baseline、projector、checkpoint 专项测试。

### Task 4: Contract 与义务覆盖（T392-T393）

**Files:** `auto_engineering/loop/output_models.py`、
`auto_engineering/loop/architect_validation.py`、`auto_engineering/gates/contract.py`、
Architect prompt/schema 及对应测试。

- [ ] RED：非对象 contract、悬空 obligation、Research 无 obligation、只有实现无测试任务
  均以稳定错误拒绝；合法实现+contract_test 通过。
- [ ] GREEN：增加 `ContractSpec`/`DesignObligation` 校验和覆盖矩阵；ContractGate 遇非对象
  值失败而非 continue；Gate/Prompt 从 ArchitectureBaseline 读取。
- [ ] VERIFY：运行 architect、contract、prompt schema 专项测试。

### Task 5: Critic 路由与修复控制（T394）

**Files:** `auto_engineering/loop/stages/design.py`、`auto_engineering/engine/state.py`、
`auto_engineering/loop/loop_budget.py`、`auto_engineering/loop/tick_orchestrator.py`、相关测试。

- [ ] RED：in-scope defect→Developer；out-of-scope/plan_gap/contract_gap→Architect；
  capability→setup；相同 Finding+相同证据达到 STAGNANT；证据变化重置 streak。
- [ ] GREEN：实现稳定 Finding fingerprint、独立 repair/stagnation/refine counters，移除
  Critic 固定 3/4 MAJOR 终止语义。
- [ ] VERIFY：运行 stage router、budget、convergence、黄金轨迹专项测试。

### Task 6: BatchReviewContext（T395）

**Files:** `auto_engineering/loop/review_context.py`、`auto_engineering/engine/state.py`、
`auto_engineering/loop/action_builder.py`、Prompt Contract 及测试。

- [ ] RED：两次 repair 后 Critic 获得去重累计文件、baseline/obligation/contract refs、Gate
  和 Finding 状态；上下文不含研究正文且满足字节预算；APPROVE 后清空批次集合。
- [ ] GREEN：实现有界投影与 ArtifactRef 退化，Developer Result 合并 batch changed files。
- [ ] VERIFY：运行 action builder、context budget、prompt duplicate 专项测试。

### Task 7: immutable challenge / Host Receipt（T396）

**Files:** `auto_engineering/host/spawn_receipt.py`、`auto_engineering/loop/action_builder.py`、
`auto_engineering/loop/tick_orchestrator.py`、Claude/Codex host assets、schemas 与测试。

- [ ] RED：challenge 文件在 Worker 完成前后 digest 不变；receipt 缺 Action/worker/result
  digest 或绑定错误均拒绝；legacy proof 在兼容期可读且给迁移提示。
- [ ] GREEN：拆分 challenge/receipt 路径；宿主协调器写 receipt；Core 校验因果与摘要。
- [ ] VERIFY：运行 spawn、worker receipt、双宿主黄金轨迹和 archive 测试。

### Task 8: 收口

- [ ] 运行 Phase 78 专项测试、全量无覆盖率测试。
- [ ] 运行 coverage ≥90%、Ruff、mypy、规则同步、metadata/check-gate。
- [ ] 更新 Spec、事故状态、BEACON、INDEX、Tracker，明确自动验证与真实产品验收边界。
- [ ] 不执行 commit、push、发布或外部真跑，除非用户另行授权。
